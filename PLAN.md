# Agenic Load-Balancer Thorough MVP Plan

## Summary
Build a macOS-only SwiftUI orchestration engine for professional multi-agent coding workflows. SwiftData is the local master repository; CloudKit provides private iCloud backup/sync/restore; `AgentNotes.md` is the project-visible coordination artifact; local CLIs are the first integration surface; API/custom-command providers are fallback. Applied resources: OpenAI docs skill, GitHub orientation skill, prior repo inspection, and official Apple/provider docs.

## Current Handoff Checkpoint
- As of May 21, 2026, Phases 0 through 6 are implemented and validated, the Phase 7 test-runner crash is fixed, and Phase 7.1 through Phase 7.6 are implemented through the persistence/execution wiring, UI repair, provider/settings/task wiring, autonomy readiness, and Settings Tools alignment closeouts described below.
- Current explicit working root for this handoff: `/Users/zincoverde/Library/Mobile Documents/com~apple~CloudDocs/4_XcodeProjects/Agenic Load-Balancer`. Do not write new work to stale OneDrive checkouts. `/Users/zincoverde/Documents/OneDrive-OLD/4_XcodeProjects/Agenic Load-Balancer` may be read only when missing historical material must be migrated.
- Current queue status: no deferred Phase 7.3-7.6 implementation task remains after the active iCloud checkout was fast-forwarded to the pushed implementation history. Sprint B in `AgentPlan.md` is implemented as an in-app Foundation Models diagnostics route, Sprint C is implemented as token-budget and continuation hardening, Sprint D is implemented as provider probe normalization plus routing telemetry hardening, Sprint E is implemented as the first Cross-Machine Conflict Center slice, Sprint F is implemented as trusted autopilot lane templates with safety review, Sprint G is implemented as the first automated UI flow/launch verification slice, Sprint H is implemented as release readiness, entitlement/preflight guardrails, and snapshot migration coverage, Sprint I is implemented as a repo-owned Developer ID release-candidate credential drill, Sprint J is implemented as provider probe maintenance plus XcodeBuildMCP source support, Sprint K is implemented as a local multi-machine conflict recovery drill, Sprint L is implemented as the first screenshot-diff visual-regression matrix, Sprint M is implemented as repo-owned live-maturity doctor scripts for provider drift, Foundation Models health, physical CloudKit drill manifests, bounded autonomy continuation checks, and signed-release credential reporting, Sprint N is implemented as physical live-release validation tooling for two-Mac CloudKit coordination, expanded visual matrices, Xcode-managed Developer ID export, and refreshed live doctors, Sprint O is implemented as provider continuation policy, expanded light/dark/maximized/provider-edge/run-sheet visual matrices, notarization resume tooling, and Solaris971 peer-bundle prep, Sprint P is implemented as continuation telemetry rollup plus provider setup status badges, Sprint Q.1 is implemented as entity-specific merge policies, Sprint Q.2 is implemented as provider remediation plus visual-regression polish for Conflict Center recovery, continuation history, provider re-probe actions, and Foundation Models diagnostics fixture states, Sprint Q.3 is implemented as the bounded autonomous loop scheduler, Sprint Q.4 is implemented as the actual restore-into-new-copy workflow with scoped SwiftData row clones, sibling-directory file copy under a configurable large-size confirmation threshold, isolated `DIVERGENCE.md`/`AgentNotes.md`/`cloned-rows.json` metadata, and a Workspace Copies management section for archive/delete actions, Sprint Q.5 is implemented as the persisted `AutonomousLoopReportRecord` plus the Autonomy control room Loop History panel with halt-reason chips and a compact iteration timeline, and Sprint Q.6 is implemented as the Loop Report Detail drill-down sheet with per-iteration cards, filter toggle, task roll-up, and a "Copy JSON" action. Future implementation should focus on completing the physical Solaris551/Solaris971 peer evidence, notarizing/stapling once a `notarytool` profile is selected, maintaining provider probes as CLIs drift, running periodic Foundation Models checks on eligible macOS 26.x machines, live end-to-end scheduler runs against a real workspace, Sprint Q.4 visual regression coverage for the Workspace Copies management section, Sprint Q.6 visual regression coverage for the Loop Report Detail sheet, and live CloudKit divergence drills against the new restore-into-new-copy workflow.
- Sprint B live Foundation Models diagnostics route status: implemented on May 21, 2026. Settings > Agents now exposes a Foundation Models Diagnostics section that checks availability and can run live command-bar metrics, guided run summary, and routing tie-break probes. Unavailable hosts skip live probes with explicit reasons; live errors classify unsupported locale/language, refusal/safety, and context-window failures without blocking deterministic routing, dispatch, AgentNotes reconciliation, or dashboard behavior.
- Sprint B validation: a full-scheme `xcodebuild test` run with isolated USB roots printed `Test run with 141 tests in 21 suites passed after 3.001 seconds`, including the new Foundation Models diagnostics tests, before the later UI-test wrapper hung and was terminated. A clean build-only pass with isolated USB roots returned `** BUILD SUCCEEDED **` using `/Volumes/USB256/Xcode_Projects_Storage/AgenicLoadBalancer/Build-20260521232102`.
- Sprint C token-budget and continuation hardening status: implemented on May 22, 2026. RunDispatcher now estimates context pressure before dispatch, compacts oversized AgentNotes preflight context, records context-budget summaries on run outcomes and usage ledgers, stores stdout/stderr transcript segments in SwiftData and snapshots, and prepares copyable continuation prompts when provider output reports context-window overflow.
- Sprint C validation: `xcodebuild test` with isolated USB roots under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintC_20260522_003925` returned `** TEST SUCCEEDED **` for `Agenic Load-BalancerTests/TokenBudgetTests` and `Agenic Load-BalancerTests/RunPipelineTests`; full app build under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintC_Build_20260522_010506` returned `** BUILD SUCCEEDED **`.
- Sprint D provider probe and routing telemetry status: implemented on May 22, 2026. Provider probes now normalize auth state, limit state, custom profile state, API-key presence, and Foundation Models no-auth state. Recent succeeded/failed/cancelled run outcomes now produce reliability snapshots, and those snapshots feed routing scores, approval sheet explanations, command-bar ranking/dispatch/probe output, autonomy run preparation, Foundation Models tie-break prompts, and dashboard heatmap cells.
- Sprint D validation: `git diff --check` produced no output. Build-only validation under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintD_20260522_032017/DerivedData_BuildOnly` returned `** BUILD SUCCEEDED **`. App launch verification passed with `DERIVED_DATA_PATH="/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintD_20260522_032017/DerivedData_RunVerify" CODE_SIGNING_ALLOWED=NO ./script/build_and_run.sh --verify`. Two focused XCTest attempts for the new Sprint D coverage stalled in Xcode orchestration before a visible `xctest` child appeared and were interrupted, so they are not claimed as passing.
- Sprint E cross-machine conflict center status: implemented on May 22, 2026. The Configure sidebar now includes Conflicts, which previews persisted conflict records and synthetic operation-log divergence, shows local/remote envelopes, affected fields, audit links, stale peer warnings, snapshot rollback warnings, and dry-run decisions for keep local, accept remote, merge, restore snapshot, and restore into a new copy. Synthetic operation-only divergence creates a backing conflict record before a user-selected decision is persisted; the first slice records decisions without mutating target entities.
- Sprint E validation: `git diff --check` produced no output. App build validation under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintE_20260522_083200` returned `** BUILD SUCCEEDED **`. Focused `ConflictResolutionEngineTests` passed; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintE_20260522_083200/Logs/Test/Test-Agenic Load-Balancer-2026.05.22_08-56-05--0400.xcresult`. The automated UI launch test passed after pinning `platform=macOS,arch=arm64`; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintE_20260522_083200/Logs/Test/Test-Agenic Load-Balancer-2026.05.22_08-57-43--0400.xcresult`. A direct launch smoke also kept the built app alive under `--uitesting` for 8 seconds.
- Sprint F trusted autopilot lanes status: implemented on May 22, 2026. `AutonomyPolicy` now includes compatibility-safe trust-lane fields for read-only review, plan-only, test-only, docs-only edits, small-file edits, dependency updates, and commit/push checkpoints. Lane templates define allowed roots, protected paths, write allowlists, command allowlists, network posture, budget caps, changed-file caps, validation commands, and rollback-evidence requirements. Autonomy validates commands before validation gates run, uses lane validation commands when drafting plans, and shows the selected lane, snapshot/checkpoint evidence, validation gate, per-task decision, and "why this is safe" review in the Autonomy control room before a run can be prepared.
- Sprint F validation: `git diff --check` produced no output. Build validation under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintF_20260522_093004` returned `** BUILD SUCCEEDED **`. Focused `AutonomyPolicyTests` and `AutonomousProjectManagerTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintF_20260522_093004/Results/SprintF_Focused.xcresult`. Automated UI launch validation passed in light and dark appearances with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintF_20260522_093004/Results/SprintF_UILaunch.xcresult`. Push evidence is recorded in the Git history for this checkpoint.
- Sprint G UI flow and launch verification status: implemented on May 22, 2026. The app now seeds deterministic `--uitesting` fixtures for configured providers, projects, active/cancelled coordination records, run outcomes, autonomy records, snapshots, and machine peers. Toolbar, Command Bar, Prompt Router, Settings Tools, Projects, project settings, workspace/task, Restore, Autonomy, and launch surfaces now expose stable accessibility identifiers without masking child controls. Workspace/task sidebar navigation now uses selection-bound `NavigationLink` rows so nested tasks open their detail panes reliably.
- Sprint G validation: `git diff --check` produced no output. Focused UI flow tests passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintG_20260522_105504/Results/SprintG_UIFlows_Final.xcresult`. Automated launch validation passed in light and dark appearances with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintG_20260522_105504/Results/SprintG_LaunchProbe_Final.xcresult`. Final build validation under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintG_20260522_105504` returned `** BUILD SUCCEEDED **`. A separate PhysicAI UI-test process was stopped during troubleshooting because it was stealing focus from this app's macOS UI tests; the clean Agenic reruns passed after focus interference was removed.
- Sprint H packaging/release readiness status: implemented on May 22, 2026. `ReleaseReadiness.md` now records the Developer ID/direct-distribution release lane, signing/notarization references, privacy/data handling posture, migration/restore gates, and rollback plan. `script/release_preflight.sh` validates release-critical build settings, CloudKit/CloudDocuments entitlement keys and values, release documentation, required Apple tooling, and optional Release build/archive flows with USB-backed output. Snapshot archives now cover the Phase 7.6 model families: provider command profiles, autonomy goals/plans/tasks/policies, machine peers, operation logs, conflict records, validation gates, and audit trails.
- Sprint H validation: `script/release_preflight.sh` passed. Focused `ReleaseReadinessTests` and `SnapshotPipelineTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintH_20260522_141338/Results/SprintH_Focused4.xcresult`. Release build validation passed with `RUN_ROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintH_20260522_141338/ReleasePreflight" script/release_preflight.sh --build`, returning `** BUILD SUCCEEDED **` and verifying the built bundle identifier. `git diff --check` produced no output before the final Sprint H closeout.
- Sprint I release-candidate drill status: implemented on May 22, 2026. `script/release_candidate.sh` now performs a credential-aware Developer ID release sequence: verify signing identity, archive, export with developer-id export options, ZIP package with `ditto`, submit with `xcrun notarytool --wait`, staple, and assess with Gatekeeper. It uses Keychain-held signing/notary credentials only and records no Apple account secrets in repo docs.
- Sprint I release validation: `bash -n script/release_candidate.sh script/release_preflight.sh` passed. `script/release_candidate.sh --verify-credentials` correctly reported the live blocker on this Mac: no `Developer ID Application` identity is currently installed; only Apple Development identities were visible. Focused `ReleaseReadinessTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintI_Release_20260522_145907/Results/ReleaseReadiness.xcresult`.
- Sprint J provider probe/XcodeBuildMCP source status: implemented on May 22, 2026. `ProviderCatalog`, `ProviderAuthRecipe`, probe telemetry, adapter defaults, and ProviderWizard regression tests now treat XcodeBuildMCP as a no-auth local tool source rather than a text-generation agent. `script/provider_probe_report.sh` creates a safe, non-secret provider/source report that checks installed binaries, version/auth diagnostics, Apple Foundation Models diagnostic posture, XcodeBuildMCP local `xcodebuild` availability, and custom-profile needs without launching installer, browser auth, device-code, or login flows.
- Sprint J validation: `bash -n script/provider_probe_report.sh script/release_candidate.sh script/release_preflight.sh` passed. XcodeBuildMCP `session_show_defaults` is callable in this Codex session and currently reports no configured project/workspace/scheme/simulator defaults. Safe probe report completed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_ProviderProbe_20260522_161704/provider-probe-report.md` with token text redacted. Focused ProviderWizard/catalog tests passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintJ_Provider_20260522_161138/Results/ProviderProbe.xcresult`.
- Sprint K multi-machine conflict recovery drill status: implemented on May 22, 2026. `ConflictRecoveryDrill` now seeds a safe synthetic two-machine scenario into SwiftData with local/remote peers, a rollback snapshot anchor, a commutative audit append conflict, and a risky task-status divergence. The Conflict Center exposes a `Run Drill` action that exercises the existing preview/resolution machinery, merges the safe audit history, plans restore into a new copy for risky divergence, records local save activity, and keeps target project/task entities untouched.
- Sprint K validation: focused `ConflictResolutionEngineTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintK_Conflict_20260522_164834/Results/ConflictDrill.xcresult`.
- Sprint L screenshot-diff visual regression status: implemented on May 25, 2026. `script/visual_regression.sh` runs a deterministic UI-test matrix for dashboard, Prompt Router, Settings Tools, Projects, and Conflict Center. The test attaches kept screenshots plus JSON visual fingerprints and asserts dimensions, color/detail buckets, luminance spread, structural contrast, and visibility geometry so blank, clipped, flattened, or shifted panes are caught early.
- Sprint L validation: `RUN_ROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintL_Visual_20260525_104900" script/visual_regression.sh` returned `** TEST SUCCEEDED **`; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintL_Visual_20260525_104900/Results/VisualRegression.xcresult`. Standalone PNG/JSON file export remains best effort because the macOS UI-test sandbox can deny arbitrary output writes; `xcresult` attachments are authoritative.
- Sprint M live-maturity doctor status: implemented on May 25, 2026. `script/live_maturity_check.sh` now orchestrates safe repo-owned checks for live provider probe drift, live Foundation Models availability and minimal response behavior, Developer ID/notary credential readiness, physical CloudKit conflict-drill manifests, and bounded-autonomy continuation safety seams. Supporting scripts are `script/provider_probe_maintenance.sh`, `script/foundation_models_check.sh`, `script/cloudkit_conflict_drill.sh`, and `script/autonomy_continuation_drill.sh`; they write reports under USB-backed run roots and do not launch installers, browser login flows, or device-code auth.
- Sprint M validation: `bash -n script/foundation_models_check.sh script/provider_probe_maintenance.sh script/cloudkit_conflict_drill.sh script/autonomy_continuation_drill.sh script/live_maturity_check.sh script/visual_regression.sh script/provider_probe_report.sh script/release_candidate.sh script/release_preflight.sh` passed. `RUN_ROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintM_LiveMaturity_20260525_110600" PROBE_TIMEOUT_SECONDS=4 script/live_maturity_check.sh` completed with provider probe maintenance, Foundation Models live check, CloudKit manifest, and bounded-autonomy drill succeeding while Developer ID/notary remained blocked by missing release credentials; report `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintM_LiveMaturity_20260525_110600/live-maturity-report.md`. Focused `ReleaseReadinessTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintM_Tests_20260525_111000/Results/ReleaseReadiness.xcresult`.
- Sprint N physical live-release validation status: implemented on May 25, 2026. `script/two_mac_cloudkit_drill.sh` now coordinates Solaris971 physical CloudKit drills with SSH/manual modes, `script/live_maturity_check.sh --two-mac-cloudkit` includes that evidence path, `script/visual_regression.sh` runs the expanded Sprint N screenshot matrix, and `script/release_candidate.sh` supports Xcode-managed Developer ID signing/export through `ALLOW_XCODE_MANAGED_SIGNING=1` and `ALLOW_PROVISIONING_UPDATES=1`.
- Sprint N validation: script syntax passed across the live-maturity and release scripts. Focused `ReleaseReadinessTests` passed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintN_Tests_20260525_123532/Results/ReleaseReadiness.xcresult`. Expanded visual regression passed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_VisualExpanded_20260525_123822/Results/VisualRegression.xcresult`. Live maturity `--all` passed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_LiveMaturity_20260525_124427/live-maturity-report.md`; Foundation Models returned `response=OK`. Developer ID export produced a hardened-runtime app/ZIP signed by `Developer ID Application: Zinco Verde, Inc. (A45694H5ZG)` under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_ReleaseManaged_20260525_121347`; Gatekeeper rejects it only as `Unnotarized Developer ID` because no `notarytool` keychain profile is visible. Solaris971 is network-addressable but refused SSH on port 22, so the peer manifest/screenshots remain the physical drill completion gate.
- Sprint O/P/Q validation closeout: the stale handoff that described uncommitted Sprint O/P/Q work on top of `e05ab7b` was reconciled against the live repository; the active iCloud checkout was already clean and pushed at `ad83322`. Focused Sprint O/P/Q tests passed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintOPQ_Tests_20260525_231201/Results/SprintOPQ.xcresult`, including ReleaseReadiness, TokenBudget, ProviderContinuationPolicy, SnapshotPipeline, ConflictResolutionEngine, UsageAndPerformance, and ProviderSetupBadge coverage. Expanded visual regression passed all selected Sprint L/N/O matrices at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintOPQ_Visual_20260525_231504/Results/VisualRegression.xcresult`. Live maturity refresh passed provider probe maintenance, Foundation Models live check, Developer ID/notary credential doctor, CloudKit conflict manifest, and bounded-autonomy continuation drill; report `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintOPQ_LiveMaturity_20260525_232024/live-maturity-report.md`.
- Sprint Q.2 provider remediation and visual-regression polish status: implemented on May 26, 2026. Provider setup now exposes `Re-probe` remediation for non-fresh health states, actionable provider rows sort ahead of healthy rows in deterministic fixtures, History rows show continuation-chain evidence, Conflict Center recovery drill status/actions have stable accessibility identifiers, and Settings > Agents can boot a deterministic Foundation Models diagnostics fixture for screenshot coverage. Focused Q.2 unit/readiness/conflict tests passed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintQ2_Tests_20260526_FINAL/Results/SprintQ2.xcresult`. Focused Q.2 UI matrix passed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintQ2_UI_20260526_005600/Results/SprintQ2UI.xcresult`. Full visual regression including Sprint L/N/O/Q.2 matrices passed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintQ2_Visual_20260526_010200/Results/VisualRegression.xcresult`.
- Last full validation command (run by OpenAI Codex on the user's macOS host on May 21, 2026): `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" -derivedDataPath "/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/DerivedData" SYMROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/Build" OBJROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/Intermediates" SHARED_PRECOMPS_DIR="/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/PrecompiledHeaders" CODE_SIGNING_ALLOWED=NO test` -> ** TEST SUCCEEDED **.
- Last passing result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/DerivedData/Logs/Test/Test-Agenic Load-Balancer-2026.05.21_18-56-06--0400.xcresult`.
- Last launch verification: `./script/build_and_run.sh --verify` built and launched `/Volumes/USB256/Xcode_Projects_Storage/Build/Products/Debug/Agenic Load-Balancer.app`; verified running PID `25667`.
- The crash fix depends on `AgenicLaunchEnvironment.usesVolatileStore(...)` treating `--uitesting`, `XCTestConfigurationFilePath`, `XCTestSessionIdentifier`, and `XCInjectBundleInto` as volatile-store signals; do not remove this while working on Foundation Models.
- A global Codex skill named `foundation-models-intelligent-features` is installed at `~/.codex/skills/foundation-models-intelligent-features` and validated. Use it for all Phase 7 Foundation Models implementation work, especially availability gates, `LanguageModelSession`, `@Generable`, streaming partials, and `Tool` calling.
- Phase 7.1 status: validated and checkpointed in `bb5468a` (`Wire Foundation Models streaming runner`). Scaffold (adapter, runner, composite, availability checker, factory, catalog entry, quota) was already in place from a prior turn. This turn replaced the Phase 7.1b placeholder with the real `LanguageModelSession.streamResponse(to:)` call behind `#if canImport(FoundationModels)` and `@available(macOS 26.0, *)`, wired `RunDispatcher` to default to `CompositeAgentRunner()` so production runs route Foundation Models commands correctly, and routed `ProviderHealthMonitor.probe` through `AgentAdapterFactory.makeAdapter` so on-device availability is probed via `SystemLanguageModel.default.availability` rather than the generic CLI binary resolver. Six new Swift Testing cases under `@Suite("Foundation Models live streaming path")` exercise the runner's snapshot→delta→line translation, end-of-stream partial flush, empty-session heartbeat, cancellation/error surfacing, the framework-unavailable fallback, and the composite-runner default path; all use a `ScriptedFoundationModelsSessionDriver` so they don't depend on Apple Intelligence being enabled. Validated by the user on May 6, 2026 (xcodebuild ** TEST SUCCEEDED **).
- Phase 7.2 status: unit-test validated and locally checkpointed in `0729c22` (`Wire Foundation Models run summaries`) on May 20, 2026. Structured `RunSummary` classification is now the baseline for Phase 7.3+ command actions, dashboard metric reads, and routing tie-break context. The app unit-test bundle passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-37-50--0400.xcresult`.
- Phase 7.3 status: unit-test validated and locally checkpointed in `30d3d9f` (`Add natural-language command bar`) on May 20, 2026. Added a toolbar command bar backed by Foundation Models tool calling with deterministic fallback, shared command action results, draft-only mutating actions, provider probes, dashboard metric reads, AgentNotes reconciliation checks, and SwiftUI sheet integration. Focused Phase 7.3 tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests/CommandBarActionTests" -only-testing:"Agenic Load-BalancerTests/CommandBarToolTests"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-47-33--0400.xcresult`. The full app unit-test bundle also passed; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-47-54--0400.xcresult`. An initial full-scheme run hit `Timed out while enabling automation mode`, but the UI/full-suite validation closeout below later completed the UI target and full scheme successfully.
- Phase 7.4 status: focused-test and build-for-testing validated, locally checkpointed in `9df9c6c` (`Add intelligent AgentNotes preflight`) on May 20, 2026. Added framework-free AgentNotes intelligence value/protocol surfaces, no-op/scripted/live Foundation Models implementations, full AgentNotes reads for prompt-relevant summaries, dispatcher injection of `promptInjectionText` over raw excerpts, and an AI-assisted merge proposal UI that keeps final AgentNotes writes confirmation-gated. Focused Phase 7.4 tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests/AgentNotesIntelligenceTests" -only-testing:"Agenic Load-BalancerTests/CoordinationCheckpointTests/readAgentNotesReturnsFullContentForIntelligentPreflight" -only-testing:"Agenic Load-BalancerTests/CoordinationCheckpointTests/composeCommandPromptPrefersIntelligentSummaryText"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-57-50--0400.xcresult`. After tightening dispatcher tests to avoid live on-device summarizer calls, `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO build-for-testing` returned `** TEST BUILD SUCCEEDED **`. Broader `xcodebuild test` attempts later stalled after building and before a visible `xctest` child appeared, so they were interrupted rather than treated as failures of app logic.
- Phase 7.5 status: build-for-testing validated and locally checkpointed in `1b8a55d` (`Add Foundation Models routing tie breaker`) on May 20, 2026. Added `RoutingTieBreak`, `RoutingTieBreakInput`, `RoutingRecommendation`, no-op/scripted/live Foundation Models tie-breakers, a close-score detector on `RoutingEngine`, and `RoutingRecommendationCoordinator` so deterministic score math remains canonical while on-device reasoning can select among close candidates only. Prompt Router now uses the recommendation wrapper, auto-selects the tie-broken provider when present, and labels the selected row with the on-device rationale while keeping the original numeric score and breakdown visible. Initial focused `xcodebuild test` / `test-without-building` attempts for `Agenic Load-BalancerTests/RoutingTieBreakerTests` stalled in Xcode test orchestration, but the later full-suite rerun completed and the Phase 7.5 tests passed.
- Phase 7.6 status: full-suite validated and locally checkpointed in `ef5b69b` (`Add autonomous project management foundation`) on May 20, 2026. Added CloudKit-compatible autonomy models for goals, plans, tasks, policies, machine peers, operation logs, conflict records, validation gates, and audit trails; registered them in `AgenicDataModel.models`; added `AutonomyPolicy`, `AutonomyPolicyEvaluator`, deterministic conflict resolution, machine sync health classification, validation gate values, and `AutonomousProjectManager` goal-to-plan drafting. Added an Autonomy sidebar section with `AutonomyControlCenterView` for project/level selection, goal intake, plan drafting, per-task policy decisions, validation commands, and local machine sync health.
- Phase 7.6 continuation status: implemented on May 20, 2026. Drafted autonomy plans now persist `AutonomyGoalRecord`, `AutonomyPlanRecord`, `AutonomyTaskRecord`, `AutonomyPolicyRecord`, operation metadata, and audit entries into SwiftData. The Autonomy control center can prepare each saved task as an approval-gated `RunPlan` through the existing approval sheet, and validation commands run only from an explicit user action through `ShellValidationGateRunner`, persisting `ValidationGateRecord` status/output and task/audit updates. Focused tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests/AutonomousProjectManagerTests" -only-testing:"Agenic Load-BalancerTests/FoundationModelsSafetyAuditTests"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_08-18-40--0400.xcresult`. Full scheme passed after this continuation with 129 tests; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_08-20-37--0400.xcresult`.
- Dispatch repair status: implemented on May 20, 2026. Codex runs now pass the composed prompt through stdin (`codex exec --json --cd <project> -`) rather than argv, `AgentProcessRunner` writes/closes stdin and terminates child processes on stream cancellation, and the approval sheet prevents accidental double-click or interactive-dismiss cancellation during active runs. Direct Codex CLI smoke test, focused ProviderWizard/RunPipeline tests, full scheme validation, and `./script/build_and_run.sh --verify` all passed; latest full result bundle is the handoff bundle above.
- UI/run-state repair status: implemented and validated on May 20, 2026. Prompt Router now adapts at smaller window widths, active runs live in a dock after the console is hidden, the command bar has an explicit close path, the iCloud toolbar button opens Storage settings after refreshing CloudKit status, Settings is now an editable in-app sheet with a shaded Liquid Glass left rail, Projects supports edit/delete/persisted folder settings, Dashboard provider metrics filter to configured providers, and provider-reported context-window or structured failures override a misleading zero process exit. Focused result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_09-55-54--0400.xcresult`. Full scheme/UI result bundle after the visual pass: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_10-05-38--0400.xcresult`. `./script/build_and_run.sh --verify` launched the Debug app as PID `57175`; visual screenshot sanity check captured `/tmp/agenic-settings-visual-pass.png`.
- Cancelled coordination cleanup status: implemented and validated on May 20, 2026. `cancelled` is now a first-class coordination status, cancelled runs no longer appear as blocked conflicts, AgentNotes generation separates active work from history, approval preflight ignores completed/checkpointed/cancelled and stale cancelled dispatch history, and the AgentNotes tab can resolve stale blocked/cancelled dispatch records back through SwiftData and regenerated AgentNotes.md. Focused result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_11-18-40--0400.xcresult`. Full scheme/UI result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_11-19-40--0400.xcresult`. `./script/build_and_run.sh --verify` launched the Debug app as PID `64612`.
- Provider auth/settings/workspace task repair status: implemented and validated on May 20, 2026. Provider setup now uses official-doc-backed account-login/API-key recipes so subscription/browser/OAuth-style CLI auth is visible separately from API-key billing. Settings and per-workspace settings now own executable policy defaults for tool calling, shell tools, network search, filesystem writes, default working path, temporary path, context compaction, and summary caps. RunDispatcher applies those policies to `RunPlan`, exports tmp/permission environment, records preprocessing time, captures Codex/Gemini-style token usage including cached and reasoning tokens, and compacts summary inputs before Foundation Models so context-window failures in the post-run summarizer do not mislabel the run. The Projects sidebar now expands into configured workspaces with nested active/previous tasks, with task details sourced from coordination events, run outcomes, and autonomy tasks. Focused ProviderWizard/RunPipeline/SnapshotPipeline result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_12-52-40--0400.xcresult`. Full scheme/UI result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_12-56-20--0400.xcresult`. `./script/build_and_run.sh --verify` launched the Debug app as PID `81151`.
- Autonomy readiness control-room polish: implemented and validated on May 21, 2026. Autonomy now has a testable readiness layer that scores workspace selection, configured provider readiness, approval policy posture, machine-sync health, saved autonomous task state, and validation gates before autonomous runs are prepared. The Autonomy screen now presents a responsive Liquid Glass control room with a readiness gauge, next-action copy, safety contract checklist, persisted autonomous work queue, validation metrics, and peer sync posture so the safe path to autonomous execution is visible instead of implicit. Focused AutonomyReadiness/AutonomousProjectManager/AutonomyPolicy result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Autonomy_20260521_172750/DerivedData/Logs/Test/Test-Agenic Load-Balancer-2026.05.21_17-27-51--0400.xcresult`. Full scheme/UI result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Autonomy_Full_20260521_173129/DerivedData/Logs/Test/Test-Agenic Load-Balancer-2026.05.21_17-31-31--0400.xcresult`. `./script/build_and_run.sh --verify` launched the Debug app as PID `69137`.
- UI/full-suite validation closeout: On May 20, 2026, `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerUITests"` returned `** TEST SUCCEEDED **` with 4 UI test executions; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_07-34-10--0400.xcresult`. The full scheme then passed with the same destination and no `-only-testing`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_07-34-56--0400.xcresult`.
- Sprint Q.3 autonomous loop scheduler status: implemented and pushed on May 26, 2026. `AutonomousLoopScheduler` walks an already-persisted autonomous plan in topological dependency order, evaluates each task against `AutonomyPolicy`, completes advisory tasks without launching providers, runs existing validation gates where present, halts on approval-required/denied/cap/deadlock conditions, and writes audit rows for every iteration. Focused Q.3 scheduler/autonomy/conflict tests passed at `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintQ3_Tests_20260526_133737/Results/SprintQ3.xcresult`.
- Sprint Q.4 restore-into-new-copy workflow status: implementation-complete on May 26, 2026. `Services/RestoreIntoNewCopyWorkflow.swift` introduces a pure `RestoreIntoNewCopyPlanner` (scoped clone plan + size estimate against an exclude list for `.git`, `node_modules`, `DerivedData`, `Build`, `.build`, `Pods`, `Carthage`, `.venv`, `target`, `dist`, `out`, `.idea`, `__pycache__`), a `@MainActor RestoreIntoNewCopyApplier` that inserts cloned `AgentProject`, `AutonomyTaskRecord`, `AutonomyOperationRecord`, and `AuditTrailRecord` rows tagged with `restore::<conflictID>::...` lineage identifiers, creates a sibling directory `<root>__divergence-<shortConflictID>` containing `DIVERGENCE.md`, an isolated `AgentNotes.md` divergence note, `cloned-rows.json`, and a recursive workspace copy (gated behind a configurable large-size confirmation threshold, default 100 MB), and a `WorkspaceCopyManager` that lists, archives (`<root>__archived`), and deletes divergence workspaces with their SwiftData clone rows. `ConflictCenterView` now wires `.restoreIntoNewCopy` to the workflow, surfaces a confirmation dialog when the planner flags a large-size copy, exposes a Workspace Copies management section with per-row Archive and Delete actions, and stops disabling the New Copy button when no snapshot anchor exists. New `RestoreIntoNewCopyWorkflowTests` covers scoped clone planning, missing-root sibling fallback, applier inserting clones without mutating source rows, sibling-directory + divergence note creation when the root exists, large-size refusal with metadata files still landing, listing/deleting divergence workspaces, archiving the sibling directory, and human-readable byte formatting. Tests still need to be executed on a macOS host because the implementation branch was prepared from the Linux-based remote execution environment.
- Sprint Q.9 loop report Markdown export status: implementation-complete on May 27, 2026. `Services/AutonomyLoopReportMarkdown.swift` adds a pure `render(_:)` entry point that decodes the persisted iteration JSON and renders halt-reason metadata, an iteration list, validation transcript excerpts (Q.7) as fenced text blocks, and a task roll-up. `AutonomyLoopReportDetailView` now exposes a "Copy Markdown" button (`Autonomy.LoopReportDetail.CopyMarkdown`) next to the existing Copy JSON action. New `AutonomyLoopReportMarkdownTests` covers header + core fields, full iteration list with transcript, empty iterations / empty rollup placeholders, and the transcript block omitted when no excerpt is captured.
- Sprint Q.8 configurable autonomy loop budget status: implementation-complete on May 27, 2026. `Services/AutonomyLoopBudgetConfig.swift` adds a `Sendable` `Codable` struct that owns the three scheduler caps plus their min/max bounds and a pure `clamped(...)` static, an `asBudget` conversion to `AutonomousLoopBudget`, and a `summary` label. `AutonomyControlCenterView` exposes three `@AppStorage` keys (`Agenic.autonomy.maxIterations`, `Agenic.autonomy.maxValidationFailures`, `Agenic.autonomy.maxApprovalsBeforeHalt`) with defaults matching `AutonomousLoopBudget.default`, a Loop Budget section in the safety panel (three bounded steppers + summary + clamping caption), and a "Run Loop" button in the work panel that's enabled only when a plan is persisted, a project root is selected, and no other validation/loop is in flight. The button invokes `AutonomousLoopScheduler` with the resolved budget and the existing `ShellValidationGateRunner`; the resulting report flows through Q.5 auto-persistence into the Loop History panel. New `AutonomyLoopBudgetConfigTests` covers default values, in-range pass-through, below-min raise, above-max lower, `asBudget` round-trip, and the summary label.
- Sprint Q.7 validation transcript excerpt status: implementation-complete on May 27, 2026. `AutonomousLoopIteration` gained a `validationOutputExcerpt: String?` field with a custom initializer defaulting to nil so existing call sites stay buildable. `CodingKeys` includes the new field; missing-key decoding falls back to nil. `AutonomousLoopScheduler.run(...)` passes `ValidationGateResult.outputExcerpt` (collapsed to nil when empty) into both validation-passed and validation-failed iteration constructors. `AutonomyLoopReportDetailView` renders the excerpt under the validation command/exit row when present (monospaced, line-limited, copy-selectable). `AutonomyLoopReportJSON.encode(_:)` adds the excerpt to the per-iteration entry when non-nil. New regression coverage: Codable round-trip with excerpt, legacy-JSON decode without the key falls back to nil, JSON encoder surfaces the excerpt, and a scheduler integration test that asserts the excerpt is captured end-to-end.
- Sprint Q.6 loop report detail drill-down status: implementation-complete on May 27, 2026. `Services/AutonomousLoopScheduler.swift` gained `AutonomyLoopReportFilter` (`all`, `failuresOnly`, `approvalsOnly`, `validationOnly`) with a pure `apply(to:)` method. `Views/AutonomyLoopReportDetailView.swift` is a new sheet-presented view with halt-reason chip, counter pills, segmented filter control, per-iteration cards (status badge, mode, detail, validation command, exit code, timestamp), task roll-up (completed vs pending), and a "Copy JSON" footer button. `AutonomyLoopReportJSON.encode(_:)` provides a stable JSON serializer. `AutonomyControlCenterView` now wraps each Loop History row in a `.plain` Button that opens the detail sheet via a `PresentedLoopReport` Identifiable wrapper. `AutonomyLoopReportDetailTests` covers all four filter modes, JSON round-trip including iteration exit codes / commands, and the empty-report case. Tests still need to be executed on a macOS host.
- Sprint Q.5 persisted loop reports status: implementation-complete on May 26, 2026. `AutonomousLoopReportRecord` is now a CloudKit-compatible SwiftData model (default-valued fields, no relationships, JSON-encoded iterations/task IDs) and is registered in `AgenicDataModel.models`. `AutonomousLoopIteration` is `Codable` so iterations round-trip through `iterationsJSON`; `AutonomousLoopHaltReason.kind` exposes the case name alongside the existing `label`. `AutonomousLoopPersistence` provides `persistReport`/`decodeIterations`/`decodeTaskIDs`. `AutonomousLoopScheduler.run(...)` now calls `persistReport` at the end of every walk (non-fatal on failure). `SnapshotPipeline.deleteAll` clears the new model so snapshot replace stays correct. `AutonomyControlCenterView` gained a Loop History panel with halt-reason chips (`completed`, `approvalRequired`, `denied`, `validationFailureCap`, `iterationCap`, `approvalCap`, `dependencyDeadlock`), iteration/validation/approval counters, and a compact tinted-dot iteration timeline rendered from the JSON. `AutonomousLoopSchedulerTests` adds report-persistence happy-path, validation-failure halt persistence, and decode tolerance coverage. Tests still need to be executed on a macOS host.
- Next recommended implementation unit: enable/run the Solaris971 peer command from the Sprint N/O two-Mac runbooks and attach the peer manifest/screenshots, then create/select a `notarytool` profile and run notarize/staple against the signed ZIP. The next pure-code units are live end-to-end scheduler runs against a real workspace, Sprint Q.4 visual regression coverage for the Workspace Copies management section, Sprint Q.6 visual regression coverage for the Loop Report Detail sheet, and live CloudKit divergence drills against the new restore-into-new-copy workflow. Provider probe maintenance, Foundation Models happy-path checks, true full-screen/baseline visual coverage, and bounded-autonomy continuation safety now have repo-owned doctor scripts or regression entry points, but they remain recurring live-release chores.

## Foundations
- Convert the Xcode project from multi-platform scaffold to macOS-only and set app/test targets to Swift 6, `SWIFT_STRICT_CONCURRENCY = complete`, and concurrency warnings-as-errors.
- Keep developer-tool posture for v1: local CLI launching enabled, selected workspace access, secrets in Keychain, no App Store sandbox promise.
- Use standard SwiftUI/AppKit controls first for Liquid Glass adoption; reserve custom glass for navigation, toolbars, inspectors, setup sheets, and floating command surfaces.
- Current environment prerequisite: build/test with full Xcode selected, for example `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`.

## Data And Sync
- SwiftData is canonical for projects, prompt threads, provider profiles, setup state, usage/cost ledgers, routing decisions, run outcomes, accuracy ratings, changelog records, plans, phases, waves, steps, task assignments, handoffs, and snapshots.
- Configure private CloudKit sync with a dedicated container, default `iCloud.com.zincoverde.Agenic-Load-Balancer`, using `ModelConfiguration(cloudKitDatabase: .private(...))`.
- Add iCloud CloudKit and Background Modes remote notifications; design CloudKit-compatible models from day one: optional relationships, explicit inverses, no unique-constraint reliance, no deny delete rules, additive schema evolution after production promotion.
- Treat CloudKit as backup/sync transport, not full historical undo. Add app-level restore snapshots, restore preview, conflict summary, and “restore into new local copy” before replacing active records.
- Keep API keys/OAuth refresh material out of SwiftData/CloudKit; store only Keychain references, auth status, provider IDs, and setup metadata.

## Concurrency Architecture
- Use actors for `RoutingEngine`, `ProviderHealthMonitor`, `AgentProcessRunner`, `UsageLedger`, `CloudSyncCoordinator`, `SnapshotRestoreCoordinator`, `ProjectCoordinationActor`, and `ProviderSetupWizard`.
- Keep all view models `@MainActor`; stream CLI output with `AsyncSequence`/`AsyncStream`; make provider events, score breakdowns, ledger events, CloudKit sync events, and dashboard snapshots `Sendable`.
- All file coordination for `AgentNotes.md` goes through one actor with atomic writes, append intents, conflict detection, and SwiftData event mirroring.

## Provider Catalog And Setup Wizard
- Ship a provider catalog seeded from official docs, with install status, version probe, auth probe, supported modes, execution command template, model list source, cost policy, quota policy, and safety notes.
- Expose local CLI setup for Codex, Claude Code, GitHub Copilot CLI, Gemini CLI, Cursor Agent, Kiro/Amazon Q path, Qwen Code, Mistral Vibe, and OpenCode; expose DeepSeek through API, OpenCode, Claude-compatible env routing, or custom command profile.
- Setup wizard supports install instructions, user-approved install commands, package-origin warnings, version verification, API key entry, environment variables, CLI login, and OAuth/browser handoffs via `ASWebAuthenticationSession`.
- Never silently run installer scripts; show source, command, expected binary, verification command, and rollback/uninstall instructions.

## Routing And Dispatch
- Dispatch mode remains “approve then run.” The app ranks agents, explains why, estimates cost/limit impact, checks availability, checks `AgentNotes.md` coordination risks, then asks for approval.
- Router scoring inputs: installed/auth status, feature support, task type, workspace write/read mode, model accuracy, latency, current session/token/call pressure, refresh window, cost, reliability, user preference, and recent failure patterns.
- Execution modes: recommend-only, read/review, plan-only, implementation, repair/debug, test/build, and commit/push checkpoint. Implementation and commit/push modes require explicit approval.

## Dashboard
- Add a Liquid Glass dashboard with heatmaps for provider/model usage, estimated cost, availability, session time, token/call limits, refresh countdowns, latency, success rate, and accuracy.
- Accuracy ledger categories: correct, minor fix needed, debug needed, recode needed, broke build/tests, abandoned, user override, and linked repair run.
- Model performance data is stored in SwiftData and synced through CloudKit, excluding secrets and raw sensitive workspace content unless the user opts into storing prompt/output excerpts.

## AgentNotes Coordination
- On project onboarding, create `AgentNotes.md` at the project root from SwiftData coordination records.
- Required preflight for every agent prompt: read `AgentNotes.md`, summarize active work, identify conflicts, claim/update task status, and respect concurrent in-progress assignments.
- Record changelog entries, implementation plans, phases, waves, steps, task owners, handoffs, blockers, decisions, test evidence, commit SHAs, push checkpoints, and rollback notes.
- If file locks or cross-agent writes become unreliable, SwiftData remains authoritative and `AgentNotes.md` becomes a regenerated/exported projection.

## Key Interfaces
- `AgentProviderProfile`: identity, install recipes, auth methods, capabilities, source URLs, supported execution modes.
- `AgentCLIAdapter`: availability probe, version probe, auth probe, command builder, stream parser, cancellation, exit classification.
- `UsageLimitPolicy`: manual quotas, provider-reported probes, refresh windows, cost estimates, pressure score.
- `RunOutcome`: status, linked repair run, correctness rating, test/build result, user feedback.
- `CoordinationEvent`: phase/wave/step, assignee, status, dependency, run ID, commit SHA, conflict marker.
- `CloudSnapshot`: version, scope, created date, record counts, checksum, restore notes.

## Implementation Phases
- Phase 0: macOS-only target cleanup, Swift 6 strict concurrency, CloudKit entitlements/config, base SwiftData schema.
- Phase 1: provider catalog, CLI adapter protocol, fake adapter test harness, Codex/Claude/Copilot initial adapters.
- Phase 2: prompt composer, routing score preview, approval sheet, process streaming, run ledger.
- Phase 3: CloudKit sync status, snapshot/restore center, conflict handling, Keychain-only secret storage.
- Phase 4: dashboard heatmaps, accuracy feedback, cost/limit policies, model performance history.
- Phase 5: setup wizard with OAuth/API/env/custom command flows and additional provider adapters.
- Phase 6: `AgentNotes.md` generation/reconciliation, cross-agent preflight prompts, commit/push checkpoint workflow.
- Phase 7: Apple Foundation Models integration. Design rule: any feature where on-device LLM functionality is suitable must consider [Apple Foundation Models](https://developer.apple.com/documentation/foundationmodels) first. Five sub-phases — implementation order optimized for incremental value:
  - Phase 7.1: in-process runner adapter so the existing run pipeline (approval sheet, live console, accuracy ledger, dashboard, snapshots) treats `apple.foundation-models` as a first-class provider. New `FoundationModelsRunner: AgentRunning` wraps `LanguageModelSession.streamResponse(to:)`, translating partial-snapshot deltas into `AgentProcessEvent.standardOutput` lines. New `FoundationModelsAdapter: AgentCLIAdapter` produces an in-process `AgentCommand`. A `CompositeAgentRunner` dispatches by `executablePath` prefix so the same dispatcher handles both child processes and on-device sessions. Availability is checked via `SystemLanguageModel.default.availability`, abstracted behind a `FoundationModelsAvailabilityChecking` protocol so the `ProviderHealthMonitor` and tests both work without requiring Apple Intelligence to be enabled in the test environment.
  - Phase 7.2: structured outcome classification using `@Generable`. After a successful run, feed stdout/stderr to a `LanguageModelSession.respond(to:generating: RunSummary.self)` call where `RunSummary` is a `@Generable struct { filesChanged: [String]; testsPassed: Int; testsFailed: Int; oneLineDescription: String; suggestedAccuracyRating: AccuracyRating }`. Stamp the structured fields onto `RunOutcomeRecord` automatically; the dashboard accuracy/performance signals get measurably better data with no regex.
  - Phase 7.3: tool-calling command bar. Expose existing app actions (`RankAgents`, `DispatchRun`, `ProbeProviders`, `CreateSnapshot`, `ReconcileAgentNotes`, `ReadDashboardMetrics`) as types conforming to the `Tool` protocol with `@Generable` `Arguments` structs. A toolbar command bar opens a `LanguageModelSession(tools: [...])` so users can describe goals in natural language ("rank agents for the failing tests, run on Codex, then commit") and the model invokes the right actions through the same actor surface the UI already uses.
  - Phase 7.4: intelligent AgentNotes preflight + reconciliation. Replace the byte-truncated 4 KB excerpt with a Foundation Models summary focused on the *current* prompt's relevance ("active claims that affect this prompt"). When `ProjectCoordinationActor.reconcile(...)` returns `.fileDiverged`, optionally ask Foundation Models to propose a merged version (the user still gates the final write through the existing `confirmationDialog`).
  - Phase 7.5: routing tie-breaker. When `RoutingEngine.rank(...)` produces multiple providers within a small score window, an on-device call returns a typed `RoutingTieBreak` (`@Generable`) that explains which provider is best for the specific prompt and why. Optional layer; deterministic scoring stays canonical, the reasoning is shown alongside the rationale string.

  Cross-cutting: every Foundation Models call site must (1) check `SystemLanguageModel.default.availability` before invoking, (2) degrade gracefully when Apple Intelligence is unavailable (show the existing fallback path, log a system line), (3) keep the framework dependency behind `#if canImport(FoundationModels)` and `@available(macOS 26.0, *)` so the project remains buildable against older SDKs even though deployment is macOS 26.4+.

## Test Plan
- Unit test routing scores, quota pressure, cost estimates, accuracy math, CloudKit-compatible model assumptions, snapshots, restore previews, and AgentNotes merge rules.
- Integration test fake adapters for missing CLI, missing auth, quota exhaustion, stream parsing, cancellation, tool failure, and malformed output.
- Concurrency test simultaneous runs updating SwiftData, CloudKit-bound records, dashboard snapshots, and AgentNotes without races.
- UI test onboarding, provider setup, OAuth handoff placeholder, prompt approval, live run output, outcome rating, restore preview, and heatmap updates.
- Build/test command: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test`.

## Sources
Official references used: [OpenAI Codex CLI](https://developers.openai.com/codex/cli), [Codex with ChatGPT plans](https://help.openai.com/en/articles/11369540-using-codex-with-your-chatgpt-plan), [Claude Code auth](https://code.claude.com/docs/en/iam), [Claude Code settings and billing lane controls](https://docs.anthropic.com/en/docs/claude-code/settings), [GitHub Copilot CLI auth](https://docs.github.com/en/copilot/how-tos/copilot-cli/set-up-copilot-cli/authenticate-copilot-cli), [GitHub Copilot CLI command reference](https://docs.github.com/en/copilot/reference/cli-command-reference), [Gemini CLI auth](https://google-gemini.github.io/gemini-cli/docs/cli/authentication.html), [Cursor Agent auth](https://docs.cursor.com/en/cli/reference/authentication), [Kiro CLI auth](https://kiro.dev/docs/cli/authentication/), [Amazon Q Developer sign-in methods](https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/getting-started-q-dev.html), [Qwen Code auth](https://qwenlm.github.io/qwen-code-docs/en/users/configuration/auth/), [Mistral Vibe configuration](https://docs.mistral.ai/mistral-vibe/terminal/configuration), [OpenCode CLI auth](https://opencode.ai/docs/cli/), [DeepSeek integrations](https://api-docs.deepseek.com/guides/coding_agents), [SwiftData CloudKit sync](https://developer.apple.com/documentation/swiftdata/syncing-model-data-across-a-persons-devices), [Swift 6 strict concurrency](https://developer.apple.com/documentation/swift/adoptingswift6), [Liquid Glass](https://developer.apple.com/documentation/TechnologyOverviews/adopting-liquid-glass), [Apple Foundation Models](https://developer.apple.com/documentation/foundationmodels), [LanguageModelSession](https://developer.apple.com/documentation/foundationmodels/languagemodelsession), [Tool protocol / tool calling](https://developer.apple.com/documentation/foundationmodels/expanding-generation-with-tool-calling), [Generating Swift data structures with guided generation](https://developer.apple.com/documentation/foundationmodels/generating-swift-data-structures-with-guided-generation).
