# AgentNotes.md

Project: Agenic Load-Balancer
Created: May 5, 2026

## Coordination Rules
- Every agent must read this file before acting in this repository.
- Summarize active constraints before edits.
- Claim in-progress work and avoid conflicting tasks.
- Record phases, waves, steps, handoffs, blockers, tests, commits, and push checkpoints.
- SwiftData is the app's canonical repository; this file is the project-visible coordination view.
- Current explicit working root as of May 21, 2026: `/Users/zincoverde/Library/Mobile Documents/com~apple~CloudDocs/4_XcodeProjects/Agenic Load-Balancer`. Do not write new data to stale OneDrive checkouts. `/Users/zincoverde/Documents/OneDrive-OLD/4_XcodeProjects/Agenic Load-Balancer` may be read only when missing historical material must be migrated.

## Active Work
- [checkpointed] Sprint I release-candidate credential drill
  Assignee: OpenAI Codex
  Detail: Added `script/release_candidate.sh` so the repo can run a real Developer ID release-candidate drill when the user's Keychain contains a `Developer ID Application` certificate and a notarytool profile. The script verifies credentials, creates a signed archive, exports with developer-id options, packages with `ditto`, submits with `xcrun notarytool --wait`, staples with `xcrun stapler`, and assesses with `spctl`. `ReleaseReadiness.md` now documents the flow and the no-secret policy for Apple account material; `ReleaseReadinessTests` guards the script contract.
  Run: local Sprint I release drill implementation and validation turn on May 22, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: `bash -n script/release_candidate.sh script/release_preflight.sh` passed. `script/release_candidate.sh --help` printed the expected option contract. `script/release_candidate.sh --verify-credentials` correctly blocked because this Mac currently has Apple Development identities but no `Developer ID Application` identity. Focused `ReleaseReadinessTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintI_Release_20260522_145907/Results/ReleaseReadiness.xcresult`.

- [checkpointed] Sprint H packaging and release readiness
  Assignee: OpenAI Codex
  Detail: Implemented the Sprint H roadmap item from `AgentPlan.md` as the release-readiness and migration-safety slice. Added `ReleaseReadiness.md` for the Developer ID/direct-distribution lane, entitlement truth, signing/notarization references, privacy/data handling, migration/restore gates, and rollback plan. Added `script/release_preflight.sh` to verify release-critical Xcode settings, CloudKit/CloudDocuments entitlement keys and values, release documentation, Apple tooling, and optional Release build/archive flows with USB-backed build output. Added release-readiness tests and expanded snapshot archives so Phase 7.6 provider command profiles, autonomy records, machine peers, operation logs, conflicts, validation gates, and audit trails are included in payload counts, identifiers, replace, merge, and schema-order coverage.
  Run: local Sprint H implementation and validation turn on May 22, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: `script/release_preflight.sh` passed. Focused `ReleaseReadinessTests` and `SnapshotPipelineTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintH_20260522_141338/Results/SprintH_Focused4.xcresult`. Release build validation passed with `RUN_ROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintH_20260522_141338/ReleasePreflight" script/release_preflight.sh --build`, returning `** BUILD SUCCEEDED **` and verifying the built app bundle identifier. `git diff --check` produced no output before the final Sprint H closeout. Work was performed from the active iCloud root; no new stale OneDrive data was written.

- [checkpointed] Sprint G UI flow and launch verification coverage
  Assignee: OpenAI Codex
  Detail: Implemented the first Sprint G automated UI flow and launch-verification slice from `AgentPlan.md`. Added deterministic `--uitesting` bootstrap fixtures for configured providers, sample workspaces, active/cancelled coordination records, a succeeded run outcome, autonomy records, snapshot data, and current machine sync state. Added stable accessibility identifiers for toolbar actions, Command Bar controls, Prompt Router controls, Settings Tools toggles, Projects controls, project settings, workspace/task detail navigation, Restore, Autonomy, and launch smoke surfaces without masking child controls. Fixed nested workspace/task sidebar navigation by using selection-bound `NavigationLink` rows instead of nested plain `Button` actions. The covered UI flows now exercise minimum-window Command Bar close behavior, Prompt Router, Settings Tools alignment, Projects controls, project settings, workspace/task navigation, Restore, Autonomy, and light/dark launch. Formal pixel-diff screenshot baselines remain in the future visual-regression queue.
  Run: local Sprint G implementation and validation turn on May 22, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: `git diff --check` produced no output. Focused Sprint G UI flow tests passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintG_20260522_105504/Results/SprintG_UIFlows_Final.xcresult`. Automated launch validation passed in light and dark appearances with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintG_20260522_105504/Results/SprintG_LaunchProbe_Final.xcresult`. Final build validation under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintG_20260522_105504` returned `** BUILD SUCCEEDED **`. Work was performed from the active iCloud root; no new stale OneDrive data was written. An unrelated PhysicAI UI-test process was stopped during troubleshooting because it was stealing focus from this app's macOS UI tests; the clean Agenic reruns passed after focus interference was removed.

- [checkpointed] Sprint F trusted autopilot lanes
  Assignee: OpenAI Codex
  Detail: Implemented the Sprint F roadmap item from `AgentPlan.md` as the trust-lane and safety-review slice. Added explicit autonomy trust lanes for read-only review, plan-only, test-only, docs-only edits, small-file edits, dependency updates, and commit/push checkpoints. `AutonomyPolicy` now persists compatibility-safe lane metadata, allowed roots, protected paths, write allowlists, command allowlists, validation commands, network policy, budget caps, changed-file caps, and rollback-evidence requirements. Mutation lanes require a successful snapshot or git checkpoint before implementation/repair/commit run preparation; dependency-update lanes require a snapshot specifically. Validation gates now reject commands outside the active lane allowlist. The Autonomy control room now lets the user choose a lane and shows rollback evidence, the selected validation gate, per-task decisions, and "why this is safe" reasoning before a run can be prepared through the existing approval sheet.
  Run: local Sprint F implementation and validation turn on May 22, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: `git diff --check` produced no output. App build validation under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintF_20260522_093004` returned `** BUILD SUCCEEDED **`. Focused `AutonomyPolicyTests` and `AutonomousProjectManagerTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintF_20260522_093004/Results/SprintF_Focused.xcresult`. Automated UI launch validation passed in light and dark appearances with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintF_20260522_093004/Results/SprintF_UILaunch.xcresult`.

- [checkpointed] Sprint E cross-machine conflict center
  Assignee: OpenAI Codex
  Detail: Implemented the Sprint E roadmap item from `AgentPlan.md`. Added a dedicated Conflicts navigation surface that builds dry-run previews from persisted `ConflictResolutionRecord` rows and synthetic divergent `AutonomyOperationRecord` pairs. The inspector shows local/remote operation envelopes, machine IDs, Lamport clocks, timestamps, affected fields, source run/task/plan links, stale peer warnings, snapshot rollback warnings, and recommended deterministic outcomes. Resolution actions cover keep local, accept remote, merge, restore snapshot, and restore into new copy. Synthetic operation-only divergence now creates a backing conflict record before the selected decision is saved, and decisions persist as `ConflictResolutionDecision` JSON without mutating target entities in this first slice.
  Run: local Sprint E implementation and validation turn on May 22, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: `git diff --check` produced no output. App build validation under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintE_20260522_083200` returned `** BUILD SUCCEEDED **`. Focused `ConflictResolutionEngineTests` passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintE_20260522_083200/Logs/Test/Test-Agenic Load-Balancer-2026.05.22_08-56-05--0400.xcresult`. The automated UI launch test passed after pinning `platform=macOS,arch=arm64`; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintE_20260522_083200/Logs/Test/Test-Agenic Load-Balancer-2026.05.22_08-57-43--0400.xcresult`. A direct launch smoke kept the freshly built app alive under `--uitesting` for 8 seconds. Earlier unpinned UI launch validation failed with `Timed out while enabling automation mode`; the pinned rerun is the current validation truth.

- [checkpointed] Sprint D provider probe and routing telemetry hardening
  Assignee: OpenAI Codex
  Detail: Implemented the Sprint D roadmap item from `AgentPlan.md`. Added normalized provider probe telemetry for auth status, API-key presence, custom command profiles, no-auth Foundation Models, and limit signals for subscription, quota, rate, and context pressure. Added recent reliability snapshots from completed run outcomes and fed them into deterministic routing scores, approval-sheet score breakdowns, command-bar rank/dispatch/probe output, autonomy run preparation, Foundation Models tie-break prompts, and dashboard heatmap cells. Dashboard and Prompt Router recommendation surfaces remain filtered to configured providers by default.
  Run: local Sprint D implementation and validation turn on May 22, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: `git diff --check` produced no output. Build-only validation from the active iCloud root with isolated Sprint D USB roots returned `** BUILD SUCCEEDED **` using `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintD_20260522_032017/DerivedData_BuildOnly`. App launch verification passed with `DERIVED_DATA_PATH="/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintD_20260522_032017/DerivedData_RunVerify" CODE_SIGNING_ALLOWED=NO ./script/build_and_run.sh --verify`. Two focused `xcodebuild test` attempts for the new Sprint D coverage stalled in Xcode orchestration before a visible `xctest` child appeared and were interrupted, so they are not recorded as passing tests.

- [checkpointed] Sprint C token budget and continuation hardening
  Assignee: OpenAI Codex
  Detail: Implemented the Sprint C roadmap item from `AgentPlan.md`. Added provider-neutral token estimation with provider context-window overrides, pre-dispatch AgentNotes compaction, context-budget summaries on approval/run outcomes, durable transcript segment records, snapshot round-trip support for transcript segments, usage-ledger context windows, and continuation prompts for context-window failures. The approval sheet now shows context pressure before dispatch, and terminal run sheets can show/copy a safe follow-up prompt when a provider fails because the context window was exceeded.
  Run: local Sprint C implementation and validation turn on May 22, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: `git diff --check` produced no output. Focused Sprint C tests passed with isolated USB roots: `xcodebuild test -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" -derivedDataPath "/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintC_20260522_003925/DerivedData" -resultBundlePath "/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintC_20260522_003925/Results/SprintC_Focused.xcresult" ... -only-testing:"Agenic Load-BalancerTests/TokenBudgetTests" -only-testing:"Agenic Load-BalancerTests/RunPipelineTests"` returned `** TEST SUCCEEDED **`. Full app build under `/Volumes/USB256/Xcode_Projects_Storage/Agenic_SprintC_Build_20260522_010506` returned `** BUILD SUCCEEDED **`.

- [checkpointed] Sprint B Foundation Models diagnostics route
  Assignee: OpenAI Codex
  Detail: Implemented the live Foundation Models verification route from `AgentPlan.md` Sprint B. Added a diagnostics runner that checks `SystemLanguageModel.default.availability`, skips live probes with explicit reasons when unavailable, and exercises command-bar metrics, guided run-summary, and close-score routing tie-break paths when the host can run Foundation Models. Wired the diagnostics into Settings > Agents so the user can observe availability, successful probes, skipped probes, unsupported locale/language, refusal/safety, context-window, and generic failure states without blocking deterministic routing or dispatch behavior.
  Run: local Sprint B implementation and validation turn on May 21, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: Swift Testing reported `Test run with 141 tests in 21 suites passed after 3.001 seconds` during the full-scheme run with isolated USB roots, including the new Foundation Models diagnostics suite, before the UI-test wrapper later hung and was terminated. Clean build-only validation with isolated USB roots returned `** BUILD SUCCEEDED **` using `/Volumes/USB256/Xcode_Projects_Storage/AgenicLoadBalancer/Build-20260521232102`.

- [checkpointed] Canonical iCloud root and expanded implementation plan
  Assignee: OpenAI Codex
  Detail: Confirmed the active project root is the iCloud checkout at `/Users/zincoverde/Library/Mobile Documents/com~apple~CloudDocs/4_XcodeProjects/Agenic Load-Balancer`, fast-forwarded it to the pushed Phase 7.3-7.6 implementation history, and replaced the stale OneDrive-oriented AgentPlan checklist with a current comprehensive implementation plan. Updated handoff truth so future agents use the iCloud checkout, keep derived/temp validation data on `/Volumes/USB256/Xcode_Projects_Storage/`, and treat OneDrive paths as read-only migration/archive material only. Preserved local project metadata and entitlements that add the developer-tool app category, include all app icon assets, and strengthen iCloud/CloudDocuments/ubiquity settings.
  Run: local canonical-root cleanup and planning turn on May 21, 2026
  Commit: this checkpoint commit
  Conflict: none
  Validation: `git diff --check` produced no output. Full scheme passed from the active iCloud root with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" -derivedDataPath "/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/DerivedData" SYMROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/Build" OBJROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/Intermediates" SHARED_PRECOMPS_DIR="/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/PrecompiledHeaders" CODE_SIGNING_ALLOWED=NO test`; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_iCloud_Canonical_20260521_185605/DerivedData/Logs/Test/Test-Agenic Load-Balancer-2026.05.21_18-56-06--0400.xcresult`. `./script/build_and_run.sh --verify` built and launched the Debug app from `/Volumes/USB256/Xcode_Projects_Storage/Build/Products/Debug/Agenic Load-Balancer.app`; verified running PID `25667`.

- [checkpointed] Settings Tools alignment polish
  Assignee: OpenAI Codex
  Detail: Reviewed the current Settings panes reported from screenshots. The Tools pane settings are live `@AppStorage` defaults, while Server, Memory, and Storage include a mix of editable defaults and read-only diagnostics/status rows. Reworked reusable settings layout so sections expand to the content column and toggle rows use a two-column settings pattern with labels on the left and switches pinned to the right. This fixes the Tools pane's compressed Default Tool Permissions card and aligns Command Bar / tool-permission switches consistently.
  Run: local UI polish and explanation turn on May 21, 2026
  Commit: this local checkpoint commit (`Align Settings tool controls`)
  Conflict: none
  Validation: `git diff --check` produced no output. Build passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" -derivedDataPath "/Volumes/USB256/Xcode_Projects_Storage/Agenic_Settings_20260521_175905/DerivedData" SYMROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_Settings_20260521_175905/Build" OBJROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_Settings_20260521_175905/Intermediates" SHARED_PRECOMPS_DIR="/Volumes/USB256/Xcode_Projects_Storage/Agenic_Settings_20260521_175905/PrecompiledHeaders" CODE_SIGNING_ALLOWED=NO build`.

- [checkpointed] Autonomy readiness control room polish
  Assignee: OpenAI Codex
  Detail: Made autonomy feel deliberate, safe, and inspectable instead of hidden behind a draft button. Added `AutonomyReadinessBuilder`, a testable readiness engine that scores workspace selection, provider readiness, policy posture, machine-sync health, active/blocked tasks, and validation gates before autonomous work can be prepared. Refined the Autonomy control center into a responsive control-room layout with a readiness gauge, next-action language, safety contract checklist, persisted autonomous work queue, validation counts, provider/task context, and machine-peer sync posture. The UI now makes the next safe step obvious: ready systems invite approved autonomous preparation, caution systems explain what needs review, and blocked systems hold execution until a concrete guardrail is repaired.
  Run: local implementation and validation turn on May 21, 2026
  Commit: this local checkpoint commit (`Polish autonomy readiness control room`)
  Conflict: none
  Validation: `git diff --check` produced no output. Focused AutonomyReadiness, AutonomousProjectManager, and AutonomyPolicy tests passed with isolated build roots; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Autonomy_20260521_172750/DerivedData/Logs/Test/Test-Agenic Load-Balancer-2026.05.21_17-27-51--0400.xcresult`. Full scheme and UI target passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" -derivedDataPath "/Volumes/USB256/Xcode_Projects_Storage/Agenic_Autonomy_Full_20260521_173129/DerivedData" SYMROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_Autonomy_Full_20260521_173129/Build" OBJROOT="/Volumes/USB256/Xcode_Projects_Storage/Agenic_Autonomy_Full_20260521_173129/Intermediates" SHARED_PRECOMPS_DIR="/Volumes/USB256/Xcode_Projects_Storage/Agenic_Autonomy_Full_20260521_173129/PrecompiledHeaders" CODE_SIGNING_ALLOWED=NO test`; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Autonomy_Full_20260521_173129/DerivedData/Logs/Test/Test-Agenic Load-Balancer-2026.05.21_17-31-31--0400.xcresult`. `./script/build_and_run.sh --verify` built and launched the Debug app; verified running PID `69137`.

- [checkpointed] Provider auth, Settings ownership, token telemetry, and workspace task navigation repair
  Assignee: OpenAI Codex
  Detail: Repaired the screenshot-reported provider, settings, run-sheet, and project/workspace gaps. Added official-doc-backed provider auth recipes that distinguish account/browser login from API-key billing lanes, and rewired the provider setup sheet to show account login commands, auth probes, API-key environment names, docs links, and advanced direct OAuth only when relevant. Converted read-only Settings into editable app defaults for tool permissions, working/tmp paths, context compaction, and summary caps; added per-workspace settings for the same execution policy so project choices persist in SwiftData/CloudKit. RunDispatcher now applies those workspace policies to run plans, exports permission/tmp-path environment, parses Codex-style `input_tokens`, `cached_input_tokens`, `output_tokens`, and `reasoning_output_tokens`, records preprocessing time, and compacts prompts/stdout/stderr before AI summaries so oversized runs no longer ask Foundation Models to summarize an unbounded transcript. The approval sheet footer now surfaces cached/reasoning/preprocessing metrics. The Projects item in the sidebar now expands into configured workspaces with nested active/previous task summaries sourced from coordination events, run outcomes, and autonomy tasks, and selecting a workspace/task opens a detail pane.
  Run: local implementation and validation turn on May 20, 2026
  Commit: this local checkpoint commit (`Wire provider auth settings and workspace tasks`)
  Conflict: none
  Validation: `git diff --check` produced no output. Focused ProviderWizard, RunPipeline, and SnapshotPipeline tests passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_12-52-40--0400.xcresult`. Full scheme and UI target passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO test`; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_12-56-20--0400.xcresult`. `./script/build_and_run.sh --verify` built and launched the Debug app; verified running PID `81151`.

- [checkpointed] Coordination cleanup / Cancelled runs, preflight filtering, and stale dispatch resolution
  Assignee: OpenAI Codex
  Detail: Added first-class `cancelled` coordination status so user-cancelled runs no longer masquerade as blocked conflicts. RunDispatcher and Autonomy task finalization now map cancelled runs to `cancelled`, and cancelled runs no longer stamp conflict markers. AgentNotes rendering now separates active work from historical completed/checkpointed/cancelled records. Approval preflight filters AgentNotes before AI summarization or prompt injection so completed, checkpointed, cancelled, and stale cancelled dispatch history are ignored. Routing and Foundation Models tie-break context now use the same active-status helper. The AgentNotes tab now exposes a per-project "Resolve Stale Dispatches" action that converts stale blocked/cancelled dispatch records to cancelled, clears cancellation conflict markers, saves SwiftData, records CloudKit activity, and regenerates AgentNotes.md from the SwiftData ledger.
  Run: local implementation and validation turn on May 20, 2026
  Commit: this local checkpoint commit (`Add cancelled coordination cleanup`)
  Conflict: none
  Validation: Focused RunPipeline, AgentNotesIntelligence, and CoordinationCheckpoint tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests/RunPipelineTests" -only-testing:"Agenic Load-BalancerTests/AgentNotesIntelligenceTests" -only-testing:"Agenic Load-BalancerTests/CoordinationCheckpointTests"`; result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_11-18-40--0400.xcresult`. Full scheme and UI target passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_11-19-40--0400.xcresult`. `./script/build_and_run.sh --verify` built and launched the Debug app; verified running PID `64612`.

- [checkpointed] UI and run-state repair / Projects CRUD, settings, CloudKit status, provider failure truth, and visual polish
  Assignee: OpenAI Codex
  Detail: Repaired the screenshot-reported app issues. Prompt Router now uses an adaptive layout instead of fixed wide columns, keeps active runs in a persistent Live Runs dock, and allows the console sheet to be hidden only when a docked return path exists. The command bar has explicit close controls. The toolbar iCloud control refreshes CloudKit state and opens the in-app Settings sheet on Storage, and the old read-only settings placeholder is now a tabbed in-app Settings sheet. Projects now have edit/delete UI: rows expose Settings and Delete, the settings sheet edits name, folder bookmark, prompt excerpt sync, and AgentNotes metadata, and every mutation saves SwiftData plus records local CloudKit activity. Dashboard heatmap, quota, accuracy, performance, and provider counts now show only configured providers. RunDispatcher now fails runs when provider output reports context-window overflow, quota/rate-limit failure, structured failure status, or nested nonzero `exit_code` even if the parent process exits zero. After the additional layout examples, applied a Liquid Glass visual pass with shaded sidebars, subtle gradients, semantic metric tints, material panels, and a Settings left rail that mirrors the supplied examples while preserving standard SwiftUI controls.
  Run: local implementation and validation turn on May 20, 2026
  Commit: this local checkpoint commit (`Repair router UI and project settings`)
  Conflict: none
  Validation: `git diff --check` produced no output. Build passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO build`. Focused RunPipeline and UsageAndPerformance tests passed with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_09-55-54--0400.xcresult`. Full scheme and UI target passed again after the visual pass with result bundle `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_10-05-38--0400.xcresult`. `./script/build_and_run.sh --verify` built and launched the Debug app; verified running PID `57175`. Visual screenshot sanity check captured `/tmp/agenic-settings-visual-pass.png`.

- [checkpointed] Dispatch repair / Codex stdin prompt transport and cancellation hardening
  Assignee: OpenAI Codex
  Detail: Reproduced the reported sub-30-second Codex CLI cancellation path. Direct `codex exec --json --cd ... "Say exactly OK and exit."` succeeded, proving the CLI invocation itself works and the red `prompt must be at most 128 characters` lines are Codex plugin/skill metadata warnings, not the user's prompt failing. Fixed the app pipeline by sending Codex prompts through stdin with `codex exec --json --cd <project> -` instead of argv, adding `AgentCommand.standardInput`, teaching `AgentProcessRunner` to write and close stdin, and terminating the child process when the stream is cancelled. Hardened the approval sheet so an active run cannot be dismissed interactively and the `Cancel run` control no longer appears where the `Run` button was, preventing accidental double-click cancellation. Benign Codex plugin/skill warning lines are now rendered as muted tooling warnings instead of fatal-looking red errors.
  Run: local defect repair turn on May 20, 2026
  Commit: this local checkpoint commit (`Harden Codex dispatch pipeline`)
  Conflict: none
  Validation: Historical validation in the then-active OneDrive checkout: direct CLI smoke test succeeded with `/Applications/Codex.app/Contents/Resources/codex exec --json --cd "/Users/zincoverde/Library/CloudStorage/OneDrive-Personal/4_XcodeProjects/Agenic Load-Balancer" "Say exactly OK and exit."`. Focused tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests/ProviderWizardTests" -only-testing:"Agenic Load-BalancerTests/RunPipelineTests"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_08-49-05--0400.xcresult`. Full scheme passed with the same destination and no `-only-testing`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_08-50-09--0400.xcresult`. `./script/build_and_run.sh --verify` then launched the fixed app as PID `44549`.

- [checkpointed] Phase 7.6 continuation / Persistence, approval-gated execution, and safety audits
  Assignee: OpenAI Codex
  Detail: Implemented the remaining Phase 7.6 wiring. `AutonomyPersistence` now persists drafted autonomy goals, plans, tasks, policy snapshots, operation metadata, and audit trail entries into SwiftData. The Autonomy control center saves drafts, shows saved plan IDs, prepares saved tasks as approval-gated `RunPlan`s through the existing approval sheet, updates task audit state after terminal runs, and runs validation commands only from an explicit user action through `ShellValidationGateRunner`, persisting `ValidationGateRecord` status/output. Added executable safety-audit coverage proving unavailable Foundation Models paths use no-op/fallback implementations and command-bar mutating actions return approval-required or blocked results instead of executing.
  Run: local implementation turn on May 20, 2026
  Commit: this local checkpoint commit (`Wire autonomy persistence and execution`)
  Conflict: none
  Validation: Focused tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests/AutonomousProjectManagerTests" -only-testing:"Agenic Load-BalancerTests/FoundationModelsSafetyAuditTests"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_08-18-40--0400.xcresult`. Full scheme passed with the same destination and no `-only-testing` (129 tests); result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_08-20-37--0400.xcresult`.

- [checkpointed] Validation / UI-test timeout closeout
  Assignee: OpenAI Codex
  Detail: Completed the previously timed-out UI validation path. The focused launch assertion, the full UI target, and the full app scheme now run to completion with the `platform=macOS,arch=arm64` destination. The UI target executed `testExample`, `testLaunchPerformance`, and `testLaunch` in both light and dark UI configurations; the full scheme also exercised all unit suites added through Phases 7.3-7.6, including RoutingTieBreaker, AutonomyPolicy, AutonomousProjectManager, ConflictResolutionEngine, MachineSyncCoordinator, and ValidationGateRunner tests.
  Run: local validation turn on May 20, 2026
  Commit: this validation-only docs checkpoint (`Record UI test validation closeout`)
  Conflict: none
  Validation: Focused UI assertion passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerUITests/Agenic_Load_BalancerUITests/testExample"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_07-33-54--0400.xcresult`. Full UI target passed with `-only-testing:"Agenic Load-BalancerUITests"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_07-34-10--0400.xcresult`. Full scheme passed with no test filter; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_07-34-56--0400.xcresult`.

- [checkpointed] Phase 7.6 / Autonomy / Policy-governed project management foundation
  Assignee: OpenAI Codex
  Detail: Implemented the Phase 7.6 foundation by adding CloudKit-compatible SwiftData records for autonomy goals, plans, tasks, policy snapshots, machine peers, operation logs, conflict-resolution records, validation gates, and audit trail entries, then registering them in `AgenicDataModel.models`. Added `AutonomyPolicy` and `AutonomyPolicyEvaluator` with observe-only, plan-only, proposed-action, approved-execution, and trusted-autopilot levels; default policy denies writes outside allowed roots, requires approval for protected paths, shell-backed runs, writes, commit/push, and over-budget runs. Added deterministic `ConflictResolutionEngine` for operation envelopes, `MachineSyncCoordinator` for peer freshness, `ValidationGateRunner` values/test doubles, and `AutonomousProjectManager` with a deterministic goal planner that creates inspect/plan/execute/validate tasks and routes every task through policy evaluation. Added an Autonomy sidebar section and `AutonomyControlCenterView` so users can select a project and level, enter a goal, draft a plan, see per-task approval decisions, validation commands, and local machine sync health. New focused tests cover policy gates, schema registration, conflict resolution, deterministic planning, validation gate results, scripted validation, and machine-sync classification.
  Run: local implementation turn on May 20, 2026
  Commit: `ef5b69b` (`Add autonomous project management foundation`)
  Conflict: none
  Validation: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO build-for-testing` returned `** TEST BUILD SUCCEEDED **`. Focused test execution was not rerun after this checkpoint because the current Xcode test runner repeatedly stalls before launching a visible `xctest` child; see the Phase 7.5 validation note for the runner behavior.

- [checkpointed] Phase 7.5 / Foundation Models / Routing tie-breaker for close provider scores
  Assignee: OpenAI Codex
  Detail: Implemented Phase 7.5 by adding `RoutingTieBreak`, `RoutingTieBreakInput`, `RoutingRecommendation`, `RoutingTieBreaking`, `NoopRoutingTieBreaker`, `ScriptedRoutingTieBreaker`, `RoutingTieBreakerFactory`, and `LiveFoundationModelsRoutingTieBreaker` behind `#if canImport(FoundationModels)` and `@available(macOS 26.0, *)`. `RoutingEngine.rank(...)` remains the canonical deterministic score path; a new `RoutingEngine.closeScoreCandidates(...)` helper finds top providers within the close-score threshold, and `RoutingRecommendationCoordinator` calls the optional tie-breaker only when more than one close candidate exists. Invalid generated provider IDs are ignored so deterministic order is preserved. Prompt Router now stores `RoutingRecommendation`, auto-selects the tie-broken provider when present, and annotates that row with the on-device rationale while keeping the original score percent, progress bar, cost, and limit-impact text visible. Added `RoutingTieBreakerTests` covering no-op fallback, close-score filtering, scripted close-score selection, invalid-provider rejection, and factory fallback.
  Run: local implementation turn on May 20, 2026
  Commit: `1b8a55d` (`Add Foundation Models routing tie breaker`)
  Conflict: none
  Validation: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO build-for-testing` returned `** TEST BUILD SUCCEEDED **`. Focused `xcodebuild test -only-testing:"Agenic Load-BalancerTests/RoutingTieBreakerTests"` first caught and fixed a SwiftUI style compile issue (`.accent` -> `Color.accentColor`), then subsequent focused `test`, `test-without-building`, and xctestrun-driven attempts stalled in Xcode orchestration before a visible `xctest` child appeared; those interrupted attempts are not counted as passing test execution.

- [checkpointed] Phase 7.4 / Foundation Models / Intelligent AgentNotes preflight and AI-assisted reconciliation
  Assignee: OpenAI Codex
  Detail: Implemented Phase 7.4 by adding `AgentNotesPreflightSummary`, `AgentNotesMergeProposal`, `AgentNotesIntelligencing`, no-op/scripted implementations, and `LiveFoundationModelsAgentNotesIntelligence` behind `#if canImport(FoundationModels)` and `@available(macOS 26.0, *)`. The approval sheet now reads full AgentNotes through the coordination actor, asks the intelligence layer for prompt-relevant active claims/conflicts/suggested claim text, displays that summary when available, and passes `promptInjectionText` into `RunDispatcher.dispatch(...)` ahead of the raw 4 KB preview fallback. `ProjectCoordinationActor` gained a full `readAgentNotes(rootPath:)` method while preserving the bounded excerpt API for fallback/preview. The AgentNotes tab now supports AI-assisted merge proposals for diverged files: proposal text, retained-line counts, unresolved conflicts, and an Apply Merge button that remains disabled until conflicts are resolved; final writes still go through the destructive confirmation dialog and `applyReconciliation(...)`. Dispatcher-oriented tests now inject `NoopRunSummarizer` in run-pipeline/checkpoint suites so unit tests never accidentally invoke live on-device summarization.
  Run: local implementation turn on May 20, 2026
  Commit: `9df9c6c` (`Add intelligent AgentNotes preflight`)
  Conflict: none
  Validation: Focused Phase 7.4 tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests/AgentNotesIntelligenceTests" -only-testing:"Agenic Load-BalancerTests/CoordinationCheckpointTests/readAgentNotesReturnsFullContentForIntelligentPreflight" -only-testing:"Agenic Load-BalancerTests/CoordinationCheckpointTests/composeCommandPromptPrefersIntelligentSummaryText"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-57-50--0400.xcresult`. After test-harness tightening, `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO build-for-testing` returned `** TEST BUILD SUCCEEDED **`. Broader `xcodebuild test` attempts stalled after building and before a visible `xctest` child appeared; interrupted attempts are not counted as passing validation.

- [checkpointed] Phase 7.3 / Foundation Models / Natural-language tool-calling command bar
  Assignee: OpenAI Codex
  Detail: Implemented Phase 7.3 by adding `CommandBarActionResult`, `CommandBarContext`, `CommandBarActionFormatter`, and a `CommandBarActionExecutor` actor that wraps the existing routing engine, provider health monitor, snapshot draft flow, AgentNotes reconciliation actor, and dashboard metric builders. Added Foundation Models `Tool` wrappers for Rank, Dispatch, Probe, Snapshot, Reconcile, and Metrics actions, all gated with `#if canImport(FoundationModels)` and `@available(macOS 26.0, *)`. Mutating actions are draft-only and return explicit approval IDs instead of executing silently. Added `NaturalLanguageCommandBarModel` with availability-aware `LanguageModelSession(tools:instructions:)` execution and deterministic fallback routing when Apple Intelligence / Foundation Models is unavailable. Added `CommandBarView` under `Views/` and wired it into the root toolbar as a sheet that builds Sendable context snapshots from the already-loaded SwiftData queries. New Swift Testing coverage exercises action formatting, ranking, dispatch approval drafts, snapshot approval drafts, dashboard metric summaries, deterministic fallback, and tool descriptions for mutating-action safety.
  Run: local implementation turn on May 20, 2026
  Commit: `30d3d9f` (`Add natural-language command bar`)
  Conflict: none
  Validation: Focused Phase 7.3 tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests/CommandBarActionTests" -only-testing:"Agenic Load-BalancerTests/CommandBarToolTests"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-47-33--0400.xcresult`. Full app unit tests passed with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests"`; result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-47-54--0400.xcresult`. Full-scheme `xcodebuild test` was attempted; all unit suites passed, but the UI-test runner failed before UI tests executed with `Timed out while enabling automation mode`. Result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-48-15--0400.xcresult`.

- [checkpointed] Phase 7.2 / Foundation Models / Structured outcome classification: Wire `@Generable RunSummary` post-success summarizer
  Assignee: Claude (via Cowork)
  Detail: Implemented Phase 7.2 by adding a Sendable `RunSummary` value type plus a `RunSummarizing` protocol so the rest of the app can reason about structured outcome classification without importing the FoundationModels framework. The live implementation `LiveFoundationModelsRunSummarizer` (gated by `#if canImport(FoundationModels)` + `@available(macOS 26.0, *)`) builds a `LanguageModelSession` with task-shaped instructions and calls `session.respond(to:generating:options:)` against a `@Generable struct GeneratedRunSummary` whose fields are annotated with `@Guide`. The model emits `filesChanged: [String]`, `testsPassed: Int`, `testsFailed: Int`, `oneLineDescription: String`, and a closed-set `GeneratedAccuracyRating` enum mirroring `AccuracyRating`; the runner translates that into a framework-independent `RunSummary` (test counts collapse to `nil` outside `.testBuild` mode). A `NoopRunSummarizer` with a localized reason is used when Apple Intelligence is unavailable, and `RunSummarizerFactory.makeDefault(...)` picks between the two via the existing `FoundationModelsAvailabilityChecking` protocol. `RunOutcomeRecord` gains six additive CloudKit-compatible fields (`aiOneLineDescription`, `aiTestsPassed`, `aiTestsFailed`, `aiFilesChangedJSON: String = "[]"`, `aiSuggestedAccuracyRating`, `aiSummaryGeneratedAt`) plus an `applyRunSummary(_:generatedAt:)` MainActor extension that stamps them while leaving the user's manual `accuracyRating` untouched. `RunOutcomeDTO` round-trips the new fields through SnapshotArchive (older archives stay decodable because all new fields are `Optional`). `RunDispatcher` accepts a `summarizer: any RunSummarizing = RunSummarizerFactory.makeDefault()` parameter, kicks off `performRunSummarization(...)` after a successful run finishes (and after the optional Phase 6 git checkpoint), and exposes a new `aiSummaryStatus: AISummaryStatus` (notRequested / pending / unavailable / succeeded / failed) so the approval sheet's outcome panel can render summary state without polling. ContentView's `outcomeRating` view gets a Liquid Glass `aiSummaryPanel` (sparkles glyph with `.symbolEffect(.breathe)` while pending, one-line description with `.textSelection(.enabled)`, tests-passed/failed chips, files-touched list) and the rating-grid buttons now use a new `AccuracyRatingButton` helper that tints + sparkles the AI-suggested rating while keeping the user's manual selection authoritative. Eight new Swift Testing cases cover RunSummary's filesChanged JSON round-trip + decode-on-error, `applyRunSummary` preserving manual rating, NoopRunSummarizer's `.unavailable` error, the factory returning Noop when availability is unavailable, RunSummaryInput's tail-truncation cap, RunOutcomeDTO round-tripping the new fields through Codable, the dispatcher stamping summary on success, dispatcher errors not degrading run outcome, dispatcher unavailable status surfacing, failed runs not invoking the summarizer, and the dispatcher correctly forwarding captured stdout/stderr buffers to the summarizer.
  Run: local implementation turn on May 6, 2026
  Commit: `0729c22` (`Wire Foundation Models run summaries`)
  Conflict: none
  Validation: Fixed `RunSummary.filesChangedJSON` to encode without escaping slashes so file-path audit text remains readable, then ran `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test -only-testing:"Agenic Load-BalancerTests"` on May 20, 2026; result: ** TEST SUCCEEDED **. Result bundle: `/Volumes/USB256/Xcode_Projects_Storage/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.20_01-37-50--0400.xcresult`.

- [checkpointed] Phase 7.1 / Foundation Models / In-process runner: Wire real LanguageModelSession.streamResponse into FoundationModelsRunner
  Assignee: Claude (via Cowork)
  Detail: Replaced the Phase 7.1b placeholder body in `FoundationModelsRunner.stream(...)` with a real `LanguageModelSession.streamResponse(to:)` invocation behind `#if canImport(FoundationModels)` and `@available(macOS 26.0, *)`. Added a new Sendable abstraction `FoundationModelsSessionDriving` with two implementations: `LiveFoundationModelsSessionDriver` (production; constructs `LanguageModelSession()` inside the spawned task and yields accumulated `partial.content` snapshots) and a scripted test stub. The runner translates accumulated snapshots into per-snapshot deltas, line-buffers them, and emits each completed line as `AgentProcessEvent.standardOutput` so the existing live console / outcome ledger / dashboard work without modification; an end-of-stream pending partial line is flushed; an empty session emits a heartbeat so usage estimation has at least one stdout line; cancellation translates to exit 130 + a stderr line; driver errors translate to exit 1 + the localized error description. `SystemLanguageModelAvailabilityChecker.currentAvailability()` now actually consults `SystemLanguageModel.default.availability` (mapped through a translator with `@unknown default` fallthrough) when the FoundationModels framework is available, otherwise returns `.frameworkUnavailable`. `RunDispatcher`'s default `runner` parameter changed from `AgentProcessRunner()` to `CompositeAgentRunner()` so production runs route `in-process://` commands to the FoundationModels runner. `ProviderHealthMonitor.probe(...)` now routes through `AgentAdapterFactory.makeAdapter(...)` so on-device availability is probed via the FoundationModels adapter rather than the generic CLI binary resolver. Six new Swift Testing cases under `@Suite("Foundation Models live streaming path")` exercise the runner's snapshot→delta→line translation, end-of-stream partial flush, empty-session heartbeat, driver error→stderr/exit-1, framework-unavailable→stderr/exit-78, and the composite runner's default routing path; all use the scripted session driver and therefore never touch real Apple Intelligence.
  Run: local implementation turn on May 5, 2026; user-side macOS xcodebuild validation on May 6, 2026
  Commit: `bb5468a` (`Wire Foundation Models streaming runner`), pushed to `origin/main`
  Conflict: none
  Validation: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test` returned ** TEST SUCCEEDED ** on the user's Mac on May 6, 2026. Result bundle: `~/Library/Developer/Xcode/DerivedData/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.06_14-33-06--0400.xcresult`.

- [checkpointed] Handoff / global Foundation Models skill registration
  Assignee: OpenAI Codex
  Detail: Created and validated the global Codex skill `foundation-models-intelligent-features` at `~/.codex/skills/foundation-models-intelligent-features`, titled "Foundation Models Adding Intelligent App Features With Generative Models". The skill is based on the local Apple sample under `tmp/AddingIntelligentAppFeaturesWithGenerativeModels`, the project-local `Skills/foundation-models` guidance, and official Apple Foundation Models documentation. It includes workflow guidance, API references, availability/fallback notes, tool-calling notes, sample patterns, and Swift examples for availability gating, itinerary planning, and points-of-interest tools. This skill lives outside the repository and is available globally to all projects.
  Run: local handoff-preparation turn on May 5, 2026
  Commit: global skill outside repo; repo implementation checkpoint before handoff docs is `d11acf9` on `main` / `origin/main`
  Conflict: none

- [checkpointed] Test runner crash / Phase 7 stabilization
  Assignee: OpenAI Codex
  Detail: Fixed `xcodebuild test` early unit-test-host crash before XCTest connection by routing XCTest-injected app launches to the in-memory SwiftData container instead of bootstrapping the CloudKit-backed store. Added a regression test for XCTest/UI-test volatile-store detection.
  Run: local implementation turn on May 5, 2026
  Commit: `d11acf9` (`Implement Agenic MVP and stabilize tests`), pushed to `origin/main`
  Conflict: none

- [checkpointed] Phase 0 / Wave 1 / Step 1: Implement thorough MVP foundation
  Assignee: OpenAI Codex
  Detail: Replaced the SwiftUI scaffold with a macOS-only orchestration console, Swift 6 strict-concurrency settings, SwiftData/CloudKit repository schema, provider catalog, routing engine, dashboard, restore center, and AgentNotes coordination.
  Run: local implementation turn
  Commit: n/a
  Conflict: none

- [checkpointed] Phase 2 / Dispatch / Run pipeline: Implement live run dispatch + approval sheet
  Assignee: Claude (via Cowork)
  Detail: Added an `AgentRunning` Sendable protocol and converted `AgentProcessRunner` to a stateless struct so a `ScriptedAgentProcessRunner` fake can be substituted. New `@MainActor @Observable RunDispatcher` owns the run lifecycle: builds the AgentCommand, persists PromptThread/Message/RoutingDecision/RunOutcome/UsageLedger/CoordinationEvent on approval, streams stdout/stderr through `AgentProcessRunner`, parses `prompt_tokens`/`completion_tokens` markers, supports user cancellation, and on finish updates the outcome status (succeeded/failed/cancelled), exit code, duration, ledger token counts, and coordination status (completed/conflict/blocked). PromptRouterView now opens a Liquid Glass approval sheet (score breakdown bars, cost & limit summary, command preview, working directory) that transitions to a live console with elapsed timer, color-coded log lines, autoscroll, token counters, Cancel button, and an outcome-rating row when the run terminates. Approval-stage build errors (missing executable, mode mismatch) are persisted as a failed RunOutcome and a blocked CoordinationEvent so the sheet renders the failure inline.
  Run: local implementation turn
  Commit: n/a
  Conflict: none

- [checkpointed] Phase 3 / Restore / Snapshot pipeline: Implement restore preview + apply + CloudKit observer
  Assignee: Claude (via Cowork)
  Detail: SwiftData ↔ JSON archive round-trip via per-`@Model` DTOs, schema-versioned envelope, SHA-256 body checksum, and typed errors. MainActor `SnapshotPipeline` builds payloads, loads in-memory preview containers, computes per-model archive/live/overlap/new diffs, and applies replace (destructive, gated) or merge (skips overlapping IDs). Restore Center UI gains Create/Import/Preview/Export/Delete actions and a Liquid Glass preview sheet with the diff grid and a `confirmationDialog`-gated destructive replace. Real archive files persist under `~/Library/Application Support/Agenic Load-Balancer/AgenicSnapshots/<id>.agenicarchive.json`. CloudKit's `.NSPersistentStoreRemoteChange` notification is now consumed via `NotificationCenter.notifications(named:)` from inside `ContentView.task`, so `CloudSyncCoordinator.recordRemoteNotification()` actually fires on remote iCloud activity (no longer a placeholder). 8 new Swift Testing cases cover round-trip, counts, preview container isolation, diff math, replace, merge with skip, tampered checksum, and unsupported schema version.
  Run: local implementation turn
  Commit: n/a
  Conflict: none

- [checkpointed] Phase 4 / Limits / Performance metrics: Implement UsageLimitPolicy + latency/success heatmap + Charts trendline
  Assignee: Claude (via Cowork)
  Detail: Extracted manual quotas, refresh windows, and pressure scoring into a Sendable `UsageLimitPolicy` value type with per-catalog defaults (calls 0.40 / tokens 0.35 / cost 0.15 / session 0.10 weighting). New `UsagePressure` exposes per-axis usage plus next refresh date. `UsageSnapshotBuilder.build` now also accepts run outcomes and produces averageLatencySeconds, successRate, and per-status counts. `DashboardMetricFactory` grew from 4 to 6 metrics per provider (added Latency and Success), and the routing engine's speed score prefers measured per-run latency. New `PerformanceHistoryBuilder` produces per-provider trend points and aggregates (success rate, avg accuracy, avg duration, avg cost). Dashboard renders a 6-column heatmap, a Liquid Glass "Limits & Quotas" panel with per-axis bars and refresh times, and a Swift Charts "Model Performance Trend" panel with one LineMark + PointMark series per provider plus per-provider summary rows. PromptRouterView passes outcomes to the routing engine so latency/success-rate signals feed scoring decisions. 11 new Swift Testing cases cover policy seeding, default fallback, linear pressure scaling, cap at 1.0, aggregate weighting, daily/manual refresh windows, snapshot builder latency + success rate, performance summary aggregation across mixed run statuses, empty-provider invariants, and dashboard heatmap metric coverage.
  Run: local implementation turn
  Commit: n/a
  Conflict: none

- [checkpointed] Phase 5 / Setup / Provider wizard: Implement install-auth-profile-verify wizard + adapter coverage
  Assignee: Claude (via Cowork)
  Detail: New `ProviderCommandProfile` `@Model` (registered in the schema) with paired Sendable `ProviderCommandProfileSnapshot` lets users override the executable, multi-line argument template (placeholders {{prompt}} {{project}} {{mode}} {{provider_id}}), and environment JSON per provider; profiles are CloudKit-syncable. `GenericCLIAdapter` now consults the profile when enabled and otherwise dispatches via per-catalog branches for Codex, Claude Code, Copilot, Gemini, Cursor, Kiro, Qwen, Mistral Vibe, OpenCode, and DeepSeek (the latter emits a descriptive placeholder so users know they need a custom profile). `RunDispatcher` resolves the active profile from SwiftData and threads it to the adapter; `adapterFactory` signature updated accordingly. `ProviderSetupWizard` actor adds `saveAPIKey` / `hasStoredAPIKey` / `clearAPIKey` (writes to macOS Keychain via the existing `KeychainVault`, returns service/account for `KeychainReferenceRecord`), and a nonisolated `defaultCommandProfileDraft(...)` helper seeded from the catalog template. `KeychainVault` gains `removeSecret`. New 4-step Liquid Glass `ProviderSetupSheet` (Install → Authenticate → Custom Profile → Verify) opens from each `ProviderProfileRow`'s Set up button and offers: install/verify command copy + probe; secure API-key field that writes to Keychain and upserts a `KeychainReferenceRecord`; browser OAuth start backed by `OAuthSignInCoordinator`/`ASWebAuthenticationSession`; env-var hint that folds into the profile editor; executable override + multi-line argument template + JSON env editor + enable toggle; final probe + summary panel. 12 new Swift Testing cases cover catalog-default arguments per provider, profile overrides, disabled-profile fallback, empty-template fallback, placeholder substitution (with nil project = empty string), valid/invalid environment JSON, blank-line filtering, and schema registration.
  Run: local implementation turn
  Commit: n/a
  Conflict: none

- [checkpointed] Phase 6 / Coordination / Checkpoint workflow: Implement NSFileCoordinator + reconciliation + git checkpoint
  Assignee: Claude (via Cowork)
  Detail: `RunOutcomeRecord` gains an additive `commitSHA: String?` (round-trips through SnapshotArchive). `ProjectCoordinationActor` wraps every read/write of AgentNotes.md with NSFileCoordinator coordinated handlers, exposes `reconcile(...)` returning a typed `AgentNotesReconciliation` (fileMissing / fileMatches / fileDiverged with SHA-256 checksums / conflictMarkers), `applyReconciliation(...)` for explicit regenerate-from-SwiftData, and `readAgentNotesExcerpt(rootPath:maxBytes:)` for preflight injection (defaults to 4 KB cap). New `Services/GitCheckpoint.swift` defines a Sendable `GitCheckpointing` protocol, `GitCheckpointResult`, `GitCheckpointError` (gitNotFound / repositoryNotInitialized / nothingToCommit / commitFailed / revParseFailed / unsupportedProjectRoot), a real `GitCheckpointCoordinator` actor that resolves git, runs `rev-parse --is-inside-work-tree` + `add -A` + `commit -m` + `rev-parse HEAD` + optional `push`, and `StubGitCheckpoint` for tests. `RunDispatcher` accepts an optional `agentNotesExcerpt` (composed above the user prompt via static `composeCommandPrompt(...)`) and a `gitCheckpoint` collaborator; after a successful run in `.commitPushCheckpoint` mode it shells out, stamps `commitSHA` on the active outcome and coordination event (status flips to `.checkpointed`), appends to AgentNotes via the coordination actor, and exposes a `CheckpointStatus` (notRequested / pending / succeeded / nothingToCommit / failed). `ApprovalSheetView` fetches the AgentNotes excerpt on appear and renders a Liquid Glass preflight panel + a checkpoint banner with status icon and "Copy SHA". `AgentNotesView` rewrites the per-project section with state badges (In sync / Missing / Diverged / Conflict), a Reconcile button, and a destructive Regenerate-from-SwiftData action gated behind `confirmationDialog`. 12 new Swift Testing cases cover ensureAgentNotes via NSFileCoordinator, reconcile state machine including divergence checksums and conflict markers, applyReconciliation, excerpt read for missing/present file, composeCommandPrompt embedding/passthrough, dispatcher checkpoint stamping commitSHA, nothing-to-commit surface, and failed runs skipping the checkpoint.
  Run: local implementation turn
  Commit: n/a
  Conflict: none

## Checkpoints
- May 20, 2026: Historical, superseded root restoration note. Restored the requested OneDrive root at `/Users/zincoverde/Library/CloudStorage/OneDrive-Personal/4_XcodeProjects/Agenic Load-Balancer` from the authorized copy-only archive at `/Users/zincoverde/Documents/OneDrive-OLD/4_XcodeProjects/Agenic Load-Balancer`, excluding Finder metadata, `tmp/`, and the stale archive Git lock. This was superseded on May 21, 2026 by the iCloud root at `/Users/zincoverde/Library/Mobile Documents/com~apple~CloudDocs/4_XcodeProjects/Agenic Load-Balancer`. Added `script/build_and_run.sh` plus `.codex/environments/environment.toml` so Codex has a reusable Run action. Validation: `./script/build_and_run.sh --verify` built the signed Debug app on the external USB build area, launched `/Volumes/USB256/Xcode_Projects_Storage/Build/Products/Debug/Agenic Load-Balancer.app`, and confirmed the process via `pgrep`.
- May 5, 2026: Initial coordination ledger created so future Codex, Claude, Copilot, DeepSeek/OpenCode, or custom-command agents have a shared handoff file before editing.
- May 5, 2026: Full macOS `xcodebuild test` passed with unit and UI tests after adding `--uitesting` in-memory SwiftData launch mode.
- May 5, 2026: Phase 2 implementation landed (Claude). New files: `Services/RunDispatcher.swift`, `Agenic Load-BalancerTests/RunPipelineTests.swift`. Modified: `Services/AgentAdapters.swift` (runner protocol + scripted fake), `ContentView.swift` (approval sheet, live console, dispatcher integration; removed unused String extensions). Added 7 Swift Testing cases covering missing CLI, missing auth + stderr capture, quota exhaustion (exit 429), malformed output, token-marker capture, successful run duration, and user cancellation.
- May 5, 2026: Phase 2 validated. `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project 'Agenic Load-Balancer.xcodeproj' -scheme 'Agenic Load-Balancer' -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test` returned ** TEST SUCCEEDED ** with the existing MVP suite plus the 7 new run-pipeline tests. Result bundle: `~/Library/Developer/Xcode/DerivedData/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.05_14-03-38--0400.xcresult`. Phase 2 is checkpointed; next-up is Phase 3 (CloudKit remote-change wiring + restore-into-new-copy preview).
- May 5, 2026: Phase 3 implementation landed (Claude). New files: `Services/SnapshotArchive.swift` (per-`@Model` Sendable Codable DTOs, `SnapshotPayload` envelope with schema version + ISO-8601 dates + sortedKeys canonical encoding + SHA-256 body checksum, typed `SnapshotArchiveError`, and `SnapshotArchiveCodec` for encode/decode/verify), `Services/SnapshotPipeline.swift` (MainActor `buildPayload` / `collectBody` / `loadPreview` / `computeDiff` / `applyReplace` / `applyMerge` plus `SnapshotDiff` and `SnapshotMergeResult`), and `Agenic Load-BalancerTests/SnapshotPipelineTests.swift` (8 cases). Modified: `Services/CoordinationAndSync.swift` to extend `SnapshotRestoreCoordinator` with archive directory creation under `~/Library/Application Support/Agenic Load-Balancer/AgenicSnapshots/`, `writeArchive`/`readArchive`/`exportArchive`/`deleteArchive`, and an optional checksum on the legacy `buildSnapshot`. `ContentView.swift` rewrote `RestoreCenterView` with Create/Import/Preview/Export/Delete actions and a Liquid Glass `RestorePreviewSheet` showing per-model diff (archive/live/overlap/new) in a `Grid` plus Apply Merge and a `confirmationDialog`-gated destructive Replace. `ContentView.task` now also awaits `observeRemoteCloudChanges()`, an async loop over `NotificationCenter.default.notifications(named: .NSPersistentStoreRemoteChange)` that calls `AppServices.cloudSync.recordRemoteNotification()` on each iCloud delivery (replacing the placeholder). Imports added: `CoreData`, `UniformTypeIdentifiers`. Build hiccup along the way: SwiftUI's `foregroundStyle(...)`/`.tint(...)` conditional that mixed `.accentColor` and `.secondary` had to be pinned to `Color.*` so the compiler stopped inferring the bare `ShapeStyle` protocol — fix is in `RestorePreviewSheet`'s diff grid and the prompt-router `RouteScoreRow`.
- May 5, 2026: Phase 3 validated. Same xcodebuild command returned ** TEST SUCCEEDED ** with the existing MVP + run-pipeline suites plus the 8 new snapshot-pipeline tests (round-trip, build-payload counts, separate preview container, diff splits, replace, merge with skip, checksum tamper, unsupported version). Result bundle: `~/Library/Developer/Xcode/DerivedData/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.05_14-41-34--0400.xcresult`. Phase 3 is checkpointed; next-up is Phase 4 (usage-limit policy actor, latency/success-rate heatmap, and accuracy feedback affordance from the live console).
- May 5, 2026: Phase 4 implementation landed (Claude). New files: `Services/UsageLimitPolicy.swift`, `Services/PerformanceHistory.swift`, `Agenic Load-BalancerTests/UsageAndPerformanceTests.swift`. Modified: `Models/AgenicModels.swift` (UsageSnapshot extended with latency/success-rate/per-status counts/optional pressure), `Services/RoutingEngine.swift` (UsageSnapshotBuilder consumes outcomes + policy; DashboardMetricFactory adds Latency + Success cells; scoreSpeed prefers measured latency), `ContentView.swift` (added `import Charts`, 6-column heatmap, Limits & Quotas panel with per-axis bars and refresh times, Model Performance Trend panel with Swift Charts LineMark+PointMark per provider, PromptRouterView passes outcomes through to routing engine; conditional `Color.*` pinning preserved). 11 new Swift Testing cases. SwiftUI Grid quirk worked around by inlining all `GridRow`s directly inside the Grid builder rather than wrapping in a separate `View` struct.
- May 5, 2026: Phase 4 validated. Same xcodebuild command returned ** TEST SUCCEEDED ** with the existing MVP + run-pipeline + snapshot-pipeline suites plus the 11 new usage/performance tests. Result bundle: `~/Library/Developer/Xcode/DerivedData/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.05_15-04-14--0400.xcresult`. Phase 4 is checkpointed; next-up is Phase 5 (provider setup wizard with OAuth/API/env/custom-command flows + adapter coverage for the remaining catalog providers).
- May 5, 2026: Phase 5 implementation landed (Claude). New files: `Agenic Load-BalancerTests/ProviderWizardTests.swift`. Modified: `Models/AgenicModels.swift` (new `ProviderCommandProfile` @Model + Sendable snapshot, schema registration), `Services/AgentAdapters.swift` (profile-aware `GenericCLIAdapter`, per-provider catalog defaults for all 10 catalog entries, `applyPlaceholders` helper), `Services/RunDispatcher.swift` (profile lookup + factory signature), `Services/CoordinationAndSync.swift` (`ProviderSetupWizard` API-key + profile-draft helpers, `KeychainVault.removeSecret`, new Sendable result types), `Agenic Load-BalancerTests/RunPipelineTests.swift` (factory signature update), `ContentView.swift` (Set up button, multi-step Liquid Glass `ProviderSetupSheet` with Install/Authenticate/Custom Profile/Verify steps, `OAuthSignInCoordinator` wired in via `ASWebAuthenticationSession`, secure API-key field that writes Keychain + `KeychainReferenceRecord`, env-var hint folded into profile, executable + argument-template + env-JSON editors, click-to-jump pip-style step indicator).
- May 5, 2026: Phase 5 validated. Same xcodebuild command returned ** TEST SUCCEEDED ** with the existing MVP + run-pipeline + snapshot-pipeline + usage/performance suites plus the 12 new provider-wizard tests. Result bundle: `~/Library/Developer/Xcode/DerivedData/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.05_15-30-01--0400.xcresult`. Phase 5 is checkpointed; next-up is Phase 6 (`AgentNotes.md` reconciliation via NSFileCoordinator + commit/push checkpoint workflow that shells out to git after run success and stamps the commit SHA on the CoordinationEventRecord).
- May 5, 2026: Phase 6 implementation landed (Claude). New files: `Services/GitCheckpoint.swift`, `Agenic Load-BalancerTests/CoordinationCheckpointTests.swift`. Modified: `Models/AgenicModels.swift` (RunOutcomeRecord.commitSHA), `Services/SnapshotArchive.swift` (RunOutcomeDTO round-trips commitSHA), `Services/CoordinationAndSync.swift` (NSFileCoordinator wrappers, reconcile / applyReconciliation / readAgentNotesExcerpt methods, AppServices.gitCheckpoint, encodingFailed CoordinationError, KeychainVault.removeSecret already from Phase 5), `Services/RunDispatcher.swift` (composeCommandPrompt static + agentNotesExcerpt parameter + CheckpointStatus + performGitCheckpoint post-run flow), `ContentView.swift` (preflight panel + checkpoint banner in ApprovalSheetView, Reconcile-per-project rebuild of AgentNotesView with state badges and confirmationDialog-gated regenerate). Build hiccup: ContentView used `\.snapshot` keypath syntax against an instance method — fixed by using `{ $0.snapshot() }` closure form.
- May 5, 2026: Phase 6 validated. Same xcodebuild command returned ** TEST SUCCEEDED ** with the existing MVP + run-pipeline + snapshot-pipeline + usage/performance + provider-wizard suites plus the 12 new coordination/checkpoint tests. Result bundle: `~/Library/Developer/Xcode/DerivedData/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.05_16-22-11--0400.xcresult`. All six MVP phases (0 through 6) are now implemented, Liquid Glass UI is wired from dashboard through restore center, prompt router/approval sheet, provider setup wizard, and AgentNotes coordination, with NSFileCoordinator-safe AgentNotes I/O and a real git-shelling checkpoint pipeline. Test totals: ~70 Swift Testing cases plus the 2 UI launch tests.
- May 5, 2026: Test-runner crash fixed after Phase 7 changes. Reproduced the failure with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project 'Agenic Load-Balancer.xcodeproj' -scheme 'Agenic Load-Balancer' -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test`; crash report showed `EXC_BREAKPOINT` on `com.apple.coredata.cloudkit.queue` inside `PFCloudKitContainerProvider containerWithIdentifier:options:` before XCTest connected. Root cause: unit-test host launches the app via XCTest injection without `--uitesting`, so bootstrap initialized the CloudKit-backed SwiftData store during tests. Fix: `AgenicLaunchEnvironment.usesVolatileStore(...)` now treats `--uitesting`, `XCTestConfigurationFilePath`, `XCTestSessionIdentifier`, and `XCInjectBundleInto` as volatile-store signals; app bootstrap uses `AgenicVolatileTestingRepository` for those runs. Added `xctestLaunchesUseVolatileStore` regression coverage. Validation: same xcodebuild command returned ** TEST SUCCEEDED **. Result bundle: `~/Library/Developer/Xcode/DerivedData/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.05_17-38-49--0400.xcresult`.
- May 5, 2026: Implementation checkpoint committed and pushed as `d11acf9` (`Implement Agenic MVP and stabilize tests`) on `main` / `origin/main`. Immediately before the handoff-doc update, `git status --short --branch` reported `## main...origin/main` with no uncommitted files.
- May 5, 2026: Global Foundation Models skill registered for all projects at `~/.codex/skills/foundation-models-intelligent-features` with `agents/openai.yaml` display metadata and validated by `python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py ~/.codex/skills/foundation-models-intelligent-features` returning `Skill is valid!`. The skill pulls from the local Apple sample bundle, the project-local Foundation Models notes, and Apple's official Foundation Models, LanguageModelSession, guided-generation, and tool-calling documentation.
- May 5, 2026: Handoff state for next agent: start by reading this file and `PLAN.md`; current recommended next work is Phase 7 Apple Foundation Models integration beginning with Phase 7.1 in-process runner adapter, while preserving the volatile-store XCTest detection that fixed the early test-runner crash.
- May 5, 2026: Phase 7.1 streaming wiring landed (Claude). New types: `FoundationModelsSessionDriving` protocol; `LiveFoundationModelsSessionDriver` (production, gated by `#if canImport(FoundationModels)` + `@available(macOS 26.0, *)`); a `ScriptedFoundationModelsSessionDriver` test stub. Modified files: `Services/FoundationModels.swift` (full rewrite of the Phase 7.1b placeholder — real `LanguageModelSession.streamResponse(to:)` call, snapshot→delta→line translation, end-of-stream partial flush, empty-session heartbeat, cancellation/error mapping, real availability checker calling `SystemLanguageModel.default.availability`), `Services/RunDispatcher.swift` (default runner = `CompositeAgentRunner()`), `Services/AgentAdapters.swift` (`ProviderHealthMonitor.probe` routes through `AgentAdapterFactory.makeAdapter` so Foundation Models availability uses on-device system model availability instead of CLI binary resolution), `Agenic Load-BalancerTests/FoundationModelsTests.swift` (added 6 streaming-path cases under `@Suite("Foundation Models live streaming path")`).
- May 5, 2026: Phase 7.1 validation pending. Cowork's bash sandbox is Linux-only and cannot run `xcodebuild`. The user must run `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test` on their Mac. Expected: prior MVP + run-pipeline + snapshot-pipeline + usage/performance + provider-wizard + coordination/checkpoint + Foundation Models integration suites all pass, plus the 6 new live-streaming cases.
- May 6, 2026: Phase 7.1 validated. The user ran the xcodebuild command above on their Mac and it returned ** TEST SUCCEEDED **. Result bundle: `~/Library/Developer/Xcode/DerivedData/Agenic_Load-Balancer-bsroquvxykavjeetekktvpnwioze/Logs/Test/Test-Agenic Load-Balancer-2026.05.06_14-33-06--0400.xcresult`. All prior suites plus the 6 new live-streaming cases pass. Phase 7.1 is checkpointed; next-up is Phase 7.2 (`@Generable RunSummary` for structured outcome classification fed by the captured stdout/stderr buffer of a successful run, stamping `filesChanged`, `testsPassed`, `testsFailed`, `oneLineDescription`, and `suggestedAccuracyRating` onto `RunOutcomeRecord`).
- May 6, 2026: Phase 7.2 implementation landed (Claude). New files: `Services/RunSummary.swift` (Sendable `RunSummary` value type + `RunSummaryInput` with tail-truncation; `RunSummarizing` protocol; `NoopRunSummarizer` + `ScriptedRunSummarizer`; `RunSummarizerFactory`; gated `@Generable GeneratedRunSummary` + `@Generable enum GeneratedAccuracyRating` mirror; `LiveFoundationModelsRunSummarizer` calling `LanguageModelSession.respond(to:generating:options:)` matching Apple's `LandmarkDescriptionView` API shape), `Agenic Load-BalancerTests/RunSummaryTests.swift` (11 cases under `@Suite("Phase 7.2 run summary")`). Modified: `Models/AgenicModels.swift` (six additive optional fields on `RunOutcomeRecord` + a MainActor extension with `applyRunSummary`, `aiFilesChanged`, `aiRunSummary`), `Services/SnapshotArchive.swift` (`RunOutcomeDTO` round-trips the new fields, optional/defaulted so older archives still decode), `Services/RunDispatcher.swift` (new `AISummaryStatus` enum, `summarizer` parameter wired to `RunSummarizerFactory.makeDefault()`, `performRunSummarization(...)` invoked from the post-success branch of the streaming task, `aiSummaryStatus` reset alongside `checkpointStatus`), `ContentView.swift` (Liquid Glass `aiSummaryPanel` with `.symbolEffect(.breathe)` while pending, four states for AISummaryStatus rendered with proper SF Symbols + colours, files-touched list capped at 8 + overflow hint, new `AccuracyRatingButton` helper that tints + sparkles the AI-suggested rating without overriding manual selection). Cross-cutting design rules from PLAN.md preserved: `#if canImport(FoundationModels)` + `@available(macOS 26.0, *)` gates around all framework references; availability is checked via `FoundationModelsAvailabilityChecking` before a session is built; failures of the summarizer NEVER degrade the run outcome — they surface as a non-blocking `AISummaryStatus.failed` / `.unavailable` instead.
- May 6, 2026: Phase 7.2 validation pending. Cowork's bash sandbox is Linux-only and cannot run `xcodebuild`. The user must run `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project "Agenic Load-Balancer.xcodeproj" -scheme "Agenic Load-Balancer" -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO test` on their Mac. Expected: prior MVP + run-pipeline + snapshot-pipeline + usage/performance + provider-wizard + coordination/checkpoint + Foundation Models integration + Foundation Models live streaming suites all pass, plus the 11 new Phase 7.2 run-summary cases.
